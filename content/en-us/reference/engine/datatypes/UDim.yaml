name: UDim
type: datatype
summary: |
  Represents a one-dimensional value with two components, a relative scale and
  an absolute offset.
description: |
  The `UDim` data type represents a one-dimensional value with two components, a
  relative scale and an absolute offset in pixels.
code_samples:
tags: []
deprecation_message: ''
constructors:
  - name: UDim.new
    summary: Returns a `UDim` from the given components.
    description: Returns a `UDim` from the given components.
    parameters:
      - name: Scale
        type: number
        default:
        summary: ''
      - name: Offset
        type: number
        default:
        summary: ''
    code_samples: []
    tags: []
    deprecation_message: ''
constants: []
properties:
  - name: UDim.Scale
    type: number
    summary: |
      The relative scale component of the `UDim`.
    description: |
      The relative scale component of the `UDim`.
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: UDim.Offset
    type: number
    summary: |
      The absolute offset component of the `UDim`.
    description: |
      The absolute offset component of the `UDim` in pixels.
    code_samples: []
    tags: []
    deprecation_message: ''
methods:
functions:
math_operations:
  - operation: +
    summary: |
      Produces a `UDim` representing the sum of the two `UDim` values.
    description: |
      Produces a `UDim` representing the sum of the two `UDim` values.
    type_a: UDim
    type_b: UDim
    return_type: UDim
    code_samples: []
    tags: []
    deprecation_message: ''
  - operation: '-'
    summary: |
      Produces a `UDim` representing the difference between the two `UDim`
      values.
    description: |
      Produces a `UDim` representing the difference between the two `UDim`
      values.
    type_a: UDim
    type_b: UDim
    return_type: UDim
    code_samples: []
    tags: []
    deprecation_message: ''
