# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: User
type: datatype
summary: |
  Represents a user's domain-scoped identity within a specific domain.
description: |
  The **User** data type represents a user's scoped identity within a specific
  domain, such as an experience or OAuth application. It encapsulates a domain
  user ID alongside the domain metadata that uniquely identifies that user
  within that domain.

  Unlike a numeric user ID alone, a `Datatype.User` value carries its domain
  context, making it unambiguous across experiences and apps. It is the standard
  identifier for referencing users in new experience code and is available from
  `Class.Player.User`.

  #### Domain User IDs

  User IDs on Roblox are **domain-scoped**: a single account has a unique domain
  user ID for each experience or app it interacts with. This ensures that user
  activity in one experience cannot be correlated with activity in another by a
  third party using the user ID alone.

  A domain user ID is an integer that is unique within a given domain
  (identified by `Enum.DomainType` and domain ID). Domain user IDs are
  guaranteed not to collide with existing global user IDs.

  #### Serialization

  `Datatype.User` supports round-trip string serialization via
  `Datatype.User:ToString()|ToString()` and
  `Datatype.User.fromString()|fromString()`. A `Datatype.User` serialized with
  `ToString()` and deserialized with `fromString()` produces an identical value.
  The encoded string is stable, URL-safe, and compact. Use it when persisting or
  transmitting user identity across systems.

  #### Users vs. Players

  A `Datatype.User` represents persistent identity, not live server presence. It
  can reference any account in the domain, whether or not that user is currently
  connected. Use `Class.Player` when you need the in-server instance.
code_samples:
  - User-Overview
tags: []
deprecation_message: ''
constructors:
  - name: User.fromId
    summary: |
      Creates a `Datatype.User` from a domain user ID within the current
      experience.
    description: |
      Creates a new `Datatype.User` from the given domain user ID, using the
      current experience as the domain. The ID must be a valid domain user ID
      (above the global user ID range).
    parameters:
      - name: domainUserId
        type: int64
        default:
        summary: |
          The domain-scoped user ID to construct the User from. Must be above
          the global user ID range.
    code_samples:
      - User-fromId
    tags: []
    deprecation_message: ''
  - name: User.fromString
    summary: |
      Creates a `Datatype.User` from its serialized string form.
    description: |
      Deserializes a `Datatype.User` from a string previously produced by
      `Datatype.User:ToString()|ToString()`.
    parameters:
      - name: userString
        type: string
        default:
        summary: |
          A string previously produced by `Datatype.User:ToString()|ToString()`.
    code_samples:
      - User-fromString
    tags: []
    deprecation_message: ''
constants: []
properties:
  - name: User.Id
    type: int64
    summary: |
      The domain user ID, unique within this domain.
    description: |
      The domain user ID for this user within the associated domain. This is an
      integer that uniquely identifies the user within the specific domain type
      and domain ID combination.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: User.DomainType
    type: DomainType
    summary: |
      The type of domain that this domain user ID belongs to.
    description: |
      The `Enum.DomainType` that identifies the kind of domain this domain user
      ID belongs to. Currently supported values are `Enum.DomainType.EXPERIENCE`
      and `Enum.DomainType.OAUTH`.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: User.DomainId
    type: int64
    summary: |
      The identifier of the specific domain instance.
    description: |
      The identifier of the specific domain instance that this user ID is scoped
      to. For `Enum.DomainType.EXPERIENCE`, this is the universe ID. For
      `Enum.DomainType.OAUTH`, this is the application ID.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
methods:
  - name: User:ToString
    summary: |
      Serializes this `Datatype.User` into a stable string representation.
    description: |
      Serializes this `Datatype.User` into a string that encodes the domain
      type, domain ID, and domain user ID. The result is stable, URL-safe, and
      compact. Use this when storing or transmitting user identity.

      The string can be converted back to a `Datatype.User` via
      `Datatype.User.fromString()|fromString()`.
    parameters: []
    returns:
      - type: string
        summary: |
          A stable, URL-safe encoded string representing the domain type, domain
          ID, and domain user ID that can be deserialized back with
          `Datatype.User.fromString()|fromString()`.
    code_samples:
      - User-ToString
    tags: []
    deprecation_message: ''
functions: []
math_operations: []
