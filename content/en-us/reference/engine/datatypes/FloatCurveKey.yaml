# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: FloatCurveKey
type: datatype
summary: |
  A time-value pair used with `Class.FloatCurve` instances.
description: |
  A time-value pair used with `Class.FloatCurve` instances.

  The `Datatype.FloatCurveKey.Interpolation|Interpolation` property dictates the
  interpolation mode for the segment started by this key and ended by the next
  key on the curve. Each segment may use a different interpolation mode.

  The `Datatype.FloatCurveKey.LeftTangent|LeftTangent` and
  `Datatype.FloatCurveKey.RightTangent|RightTangent` properties apply to the
  cubic interpolation mode and define the desired tangent (slope) at the key.
  Different left and right values can be used to encode discontinuities in slope
  at the key. Attempting to set a
  `Datatype.FloatCurveKey.RightTangent|RightTangent` value on a key that doesn't
  use the cubic interpolation mode will result in a runtime error. It is
  possible to set the `Datatype.FloatCurveKey.LeftTangent|LeftTangent` property
  on any key, as it will be used should the preceding segment use cubic
  interpolation.
code_samples: []
tags: []
deprecation_message: ''
constructors:
  - name: FloatCurveKey.new
    summary: |
      Returns a new `Datatype.FloatCurveKey` from the given time and value.
    description: |
      Creates a new `Datatype.FloatCurveKey` at a given time and value.
      `Datatype.FloatCurveKey.LeftTangent|LeftTangent` and
      `Datatype.FloatCurveKey.RightTangent|RightTangent` are left uninitialized
      and, if not initialized, tangent values of 0 will be used when evaluating
      the curve.
    parameters:
      - name: time
        type: number
        default:
        summary: |
          Time at which to create the new `Datatype.FloatCurveKey`.
      - name: value
        type: number
        default:
        summary: |
          Value of the new `Datatype.FloatCurveKey`.
      - name: Interpolation
        type: KeyInterpolationMode
        default:
        summary: |
          The interpolation mode for the curve segment starting at this key.
          Defaults to `Enum.KeyInterpolationMode|Cubic` when omitted.
    code_samples: []
    tags: []
    deprecation_message: ''
constants: []
properties:
  - name: FloatCurveKey.Interpolation
    type: KeyInterpolationMode
    summary: |
      The key interpolation mode for the segment started by this
      `Datatype.FloatCurveKey`.
    description: |
      Specifies the interpolation mode used for the curve segment that begins at
      this key and ends at the next key on the `Class.FloatCurve`. Each segment
      of the curve can use a different interpolation mode. The default value is
      `Enum.KeyInterpolationMode.Cubic`.

      The available modes are:

      - **Constant** — the segment holds the value of this key until the next
        key.
      - **Linear** — the segment linearly interpolates between this key's value
        and the next key's value.
      - **Cubic** — the segment uses a cubic Hermite spline defined by this
        key's `Datatype.FloatCurveKey.RightTangent|RightTangent` and the next
        key's `Datatype.FloatCurveKey.LeftTangent|LeftTangent`.

      Changing this property to a non-cubic mode automatically clears the
      `Datatype.FloatCurveKey.RightTangent|RightTangent` of this key, because
      only cubic segments use a right tangent.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: FloatCurveKey.Time
    type: number
    summary: |
      The time position of this `Datatype.FloatCurveKey`.
    description: |
      The time position of this `Datatype.FloatCurveKey` along the curve's
      timeline. The default value is `0`. Keys within a `Class.FloatCurve` are
      kept sorted in ascending time order; if you insert a key at a time that
      already exists on the curve, the existing key at that time is replaced.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: FloatCurveKey.Value
    type: number
    summary: |
      The value of this `Datatype.FloatCurveKey`.
    description: |
      The numerical output value at this key's position on the curve. The
      default value is `0`. When a `Class.FloatCurve` is sampled at this key's
      `Datatype.FloatCurveKey.Time|Time`, the curve evaluates to this value
      (subject to any interpolation with neighboring keys depending on the
      sampling time).
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: FloatCurveKey.RightTangent
    type: number
    summary: |
      The tangent to the right of this `Datatype.FloatCurveKey`.
    description: |
      The slope of the curve leaving this key toward the next key. The default
      value is `nil`, which means the tangent is automatically computed (an
      effective value of `0` is used during evaluation). This property can only
      be set when `Datatype.FloatCurveKey.Interpolation|Interpolation` is
      `Enum.KeyInterpolationMode.Cubic`; attempting to set it on a key with
      Constant or Linear interpolation raises a runtime error. Pairing a
      different RightTangent value on this key with a different
      `Datatype.FloatCurveKey.LeftTangent|LeftTangent` value on the next key
      creates a discontinuity in slope (a "corner") at the next key.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: FloatCurveKey.LeftTangent
    type: number
    summary: |
      The tangent to the left of this `Datatype.FloatCurveKey`.
    description: |
      The slope of the curve arriving at this key from the previous key. The
      default value is `nil`, which means the tangent is automatically computed
      (an effective value of `0` is used during evaluation). Unlike
      `Datatype.FloatCurveKey.RightTangent|RightTangent`, this property can be
      set on a key with any interpolation mode, because the preceding segment
      may itself use cubic interpolation and therefore needs a defined left
      tangent at this endpoint.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
methods: []
functions: []
math_operations: []
