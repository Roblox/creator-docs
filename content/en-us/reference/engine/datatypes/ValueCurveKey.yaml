# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: ValueCurveKey
type: datatype
summary: |
  A time-value pair used with `Class.ValueCurve` instances.
description: |
  A time-value pair used with `Class.ValueCurve` instances.

  The `Datatype.ValueCurveKey.Interpolation|Interpolation` property dictates the
  interpolation mode for the segment started by this key and ended by the next
  key on the curve. Each segment may use a different interpolation mode.

  The `Datatype.ValueCurveKey.LeftTangent|LeftTangent` and
  `Datatype.ValueCurveKey.RightTangent|RightTangent` properties apply to the
  cubic interpolation mode and define the desired tangent (slope) at the key.
  Different left and right values can be used to encode discontinuities in slope
  at the key. Attempting to set a
  `Datatype.ValueCurveKey.RightTangent|RightTangent` value on a key that doesn't
  use the cubic interpolation mode will result in a runtime error. It is
  possible to set the `Datatype.ValueCurveKey.LeftTangent|LeftTangent` property
  on any key, as it will be used should the preceding segment use cubic
  interpolation.
code_samples: []
tags: []
deprecation_message: ''
constructors:
  - name: ValueCurveKey.new
    summary: |
      Returns a new `Datatype.ValueCurveKey` from the given time and value.
    description: |
      Creates a new `Datatype.ValueCurveKey` at a given time and value.
      `Datatype.ValueCurveKey.LeftTangent|LeftTangent` and
      `Datatype.ValueCurveKey.RightTangent|RightTangent` are left uninitialized
      and, if not initialized, tangent values of '0' will be used when
      evaluating the curve.
    parameters:
      - name: time
        type: number
        default:
        summary: |
          Time at which to create the new `Datatype.ValueCurveKey`.
      - name: value
        type: any
        default:
        summary: |
          Value of the new `Datatype.ValueCurveKey`.
      - name: Interpolation
        type: KeyInterpolationMode
        default:
        summary: |
          The interpolation mode for the curve segment starting at this key.
          Defaults to `Enum.KeyInterpolationMode.Constant` if omitted.
    code_samples: []
    tags: []
    deprecation_message: ''
constants: []
properties:
  - name: ValueCurveKey.Interpolation
    type: KeyInterpolationMode
    summary: |
      The key interpolation mode for the segment started by this
      `Datatype.ValueCurveKey`.
    description: |
      Specifies the interpolation mode used for the curve segment that begins at
      this key and ends at the next key on the `Class.ValueCurve`. Each segment
      of the curve can use a different interpolation mode. The default value is
      `Enum.KeyInterpolationMode.Constant`.

      The available modes are:

      - **Constant** — the segment holds the value of this key until the next
        key.
      - **Linear** — the segment linearly interpolates between this key's value
        and the next key's value.
      - **Cubic** — the segment uses a cubic Hermite spline defined by this
        key's `Datatype.ValueCurveKey.RightTangent|RightTangent` and the next
        key's `Datatype.ValueCurveKey.LeftTangent|LeftTangent`.

      Not all value types support Linear or Cubic interpolation. Non-numerical
      types such as strings, booleans, and enums revert to Constant
      interpolation regardless of this setting.

      Changing this property to a non-cubic mode automatically clears the
      `Datatype.ValueCurveKey.RightTangent|RightTangent` of this key, because
      only cubic segments use a right tangent.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: ValueCurveKey.Time
    type: number
    summary: |
      The time position of this `Datatype.ValueCurveKey`.
    description: |
      The time position of this `Datatype.ValueCurveKey` along the curve.
      Defaults to `0`. Keys within a `Class.ValueCurve` are stored sorted by
      their time values; the time determines where in the animation timeline
      this key's value applies.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: ValueCurveKey.Value
    type: any
    summary: |
      The value of this `Datatype.ValueCurveKey`.
    description: |
      The value stored in this `Datatype.ValueCurveKey`. Defaults to `nil`. Can
      hold any supported Luau type, including numbers, booleans, strings,
      `Datatype.Vector2`, `Datatype.Vector3`, `Datatype.CFrame`,
      `Datatype.UDim`, `Datatype.UDim2`, `Datatype.NumberRange`,
      `Datatype.NumberSequence`, `Datatype.ColorSequence`, and enums. All keys
      on a single `Class.ValueCurve` must hold values of the same type;
      attempting to insert a key whose value type differs from existing keys
      produces an error.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: ValueCurveKey.RightTangent
    type: number
    summary: |
      The tangent to the right of this `Datatype.ValueCurveKey`.
    description: |
      The outgoing tangent (slope) at this key, used when the
      `Datatype.ValueCurveKey.Interpolation|Interpolation` is
      `Enum.KeyInterpolationMode.Cubic`. Defaults to `nil` (unset); when unset,
      an effective tangent of `0` is used during curve evaluation. Attempting to
      set this property on a key whose interpolation mode is not Cubic throws a
      runtime error. This property is automatically cleared when
      `Datatype.ValueCurveKey.Interpolation|Interpolation` is changed to a
      non-cubic mode.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: ValueCurveKey.LeftTangent
    type: number
    summary: |
      The tangent to the left of this `Datatype.ValueCurveKey`.
    description: |
      The incoming tangent (slope) at this key, used when the preceding segment
      uses `Enum.KeyInterpolationMode.Cubic` interpolation. Defaults to `nil`
      (unset); when unset, an effective tangent of `0` is used during curve
      evaluation. Unlike `Datatype.ValueCurveKey.RightTangent|RightTangent`,
      this property can be set on any key regardless of its own interpolation
      mode, because it defines the slope at the endpoint of the preceding cubic
      segment.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
methods: []
functions: []
math_operations: []
