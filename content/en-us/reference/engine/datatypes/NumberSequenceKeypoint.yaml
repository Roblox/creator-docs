# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: NumberSequenceKeypoint
type: datatype
summary: |
  Represents keypoint within a `Datatype.NumberSequence` with a particular time,
  value, and envelope size.
description: |
  The `Datatype.NumberSequenceKeypoint` data type represents keypoints within a
  NumberSequence with a particular time, value, and envelope size.
code_samples: []
tags: []
deprecation_message: ''
constructors:
  - name: NumberSequenceKeypoint.new
    summary: |
      Returns a keypoint with the specified time and value.
    description: |
      Returns a `Datatype.NumberSequenceKeypoint` with the given `time` and
      `value`, with `Datatype.NumberSequenceKeypoint.Envelope|Envelope`
      defaulting to 0 (no random variance). The `time` parameter should be a
      number between 0 and 1, and `value` can be any number.
    parameters:
      - name: time
        type: number
        default:
        summary: |
          The relative time position of the keypoint within the sequence,
          expressed as a number from 0 to 1.
      - name: value
        type: number
        default:
        summary: |
          The base numeric value at this keypoint's time position.
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: NumberSequenceKeypoint.new
    summary: |
      Returns a keypoint with the specified time, value, and envelope.
    description: |
      Returns a `Datatype.NumberSequenceKeypoint` with the given `time`,
      `value`, and `envelope`. The `envelope` parameter must be a non-negative
      number that defines the amount of random variance applied to `value` at
      runtime; the actual output for this keypoint is chosen randomly within
      [value&nbsp;&minus;&nbsp;envelope,&nbsp;value&nbsp;+&nbsp;envelope].
    parameters:
      - name: time
        type: number
        default:
        summary: |
          The relative time position of the keypoint within the sequence,
          expressed as a number from 0 to 1.
      - name: value
        type: number
        default:
        summary: |
          The base numeric value at this keypoint's time position.
      - name: envelope
        type: number
        default:
        summary: |
          The amount of random variance that can be applied to the value at
          runtime; must be non-negative.
    code_samples: []
    tags: []
    deprecation_message: ''
constants: []
properties:
  - name: NumberSequenceKeypoint.Envelope
    type: number
    summary: |
      The amount of variance allowed from the value.
    description: |
      The amount of variance allowed from the value. For example, a value of 0.5
      with an envelope of 0.1 could compute to between 0.4 and 0.6 at runtime.
      The most common use case for this property is to randomize the size of
      particles (within the envelope) from a
      `Class.ParticleEmitter|ParticleEmitter`.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: NumberSequenceKeypoint.Time
    type: number
    summary: |
      The relative time at which the keypoint is positioned.
    description: |
      The relative time at which this keypoint is positioned within the parent
      `Datatype.NumberSequence`, expressed as a number from 0 to 1 where 0 is
      the beginning of the sequence and 1 is the end. Within a
      `Datatype.NumberSequence`, the first keypoint must have a time of 0 and
      the last keypoint must have a time of 1; any intermediate keypoints must
      be in ascending time order. The engine linearly interpolates values and
      envelopes between consecutive keypoints based on their time positions.
      Defaults to 0.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: NumberSequenceKeypoint.Value
    type: number
    summary: |
      The base value of the keypoint.
    description: |
      The base numeric value at this keypoint's position in the sequence. When
      the engine evaluates a `Datatype.NumberSequence`, it linearly interpolates
      between adjacent keypoints' values based on time. If
      `Datatype.NumberSequenceKeypoint.Envelope|Envelope` is greater than 0, the
      actual output at runtime may vary randomly within the range
      [Value&nbsp;&minus;&nbsp;Envelope,&nbsp;Value&nbsp;+&nbsp;Envelope].
      Defaults to 0.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
methods: []
functions: []
math_operations: []
