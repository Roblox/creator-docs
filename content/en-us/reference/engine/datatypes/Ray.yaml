# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: Ray
type: datatype
summary: |
  Represents a line with a starting point that casts infinitely in a specific
  direction.
description: |
  The `Datatype.Ray` data type represents a half-line, finite in one direction
  but infinite in the other. It can be defined by a 3D point, where the line
  originates from, and a direction vector, which is the direction it goes in.
code_samples: []
tags: []
deprecation_message: ''
constructors:
  - name: Ray.new
    summary: |
      Returns a `Datatype.Ray` with the given `Origin` and `Direction`.
    description: |
      Returns a new `Datatype.Ray` with given `Origin` and `Direction`.
    parameters:
      - name: Origin
        type: Vector3
        default:
        summary: |
          The starting point of the ray in 3D space.
      - name: Direction
        type: Vector3
        default:
        summary: |
          The direction vector of the ray. Does not need to be a unit vector.
    code_samples: []
    tags: []
    deprecation_message: ''
constants: []
properties:
  - name: Ray.Unit
    type: Ray
    summary: |
      The `Datatype.Ray` with a normalized direction (the direction has a
      magnitude of `1`).
    description: |
      The `Datatype.Ray` with a normalized direction (the direction has a
      magnitude of `1`).
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Ray.Origin
    type: Vector3
    summary: |
      The position of the origin.
    description: |
      The position of the origin.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Ray.Direction
    type: Vector3
    summary: |
      The direction vector of the `Datatype.Ray`.
    description: |
      The direction vector of the `Datatype.Ray`.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
methods:
  - name: Ray:ClosestPoint
    summary: |
      Returns a `Datatype.Vector3` projected onto the ray so that it is within
      the `Datatype.Ray` line of sight.
    description: |
      Returns a `Datatype.Vector3` projected onto the ray so that it is within
      the `Datatype.Ray` line of sight.

      **Note:** the `Datatype.Ray` **must** be a unit ray for this method to
      behave as expected!
    parameters:
      - name: point
        type: Vector3
        default:
        summary: |
          The point in 3D space to project onto the ray.
    returns:
      - type: Vector3
        summary: |
          The point on the ray closest to the given point, or the ray origin if
          the projection falls behind it.
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Ray:Distance
    summary: |
      Returns the distance between the given point and the closest point on the
      `Datatype.Ray`.
    description: |
      Returns the distance between the given point and the point on the ray
      nearest to the given point (`Datatype.Ray.ClosestPoint(point)`).
    parameters:
      - name: point
        type: Vector3
        default:
        summary: |
          The point in 3D space to measure distance from.
    returns:
      - type: number
        summary: |
          The shortest distance between the given point and the closest point on
          the ray.
    code_samples: []
    tags: []
    deprecation_message: ''
functions: []
math_operations: []
