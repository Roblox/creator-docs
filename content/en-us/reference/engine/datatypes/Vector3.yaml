# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: Vector3
type: datatype
summary: |
  Represents a 3D value with a direction and magnitude.
description: |
  The `Datatype.Vector3` data type represents a vector in 3D space, typically
  used as a point in 3D space or the dimensions of a rectangular prism.
  `Datatype.Vector3` supports basic component-based arithmetic operations (sum,
  difference, product, and quotient) and these operations can be applied on the
  left or right hand side to either another `Datatype.Vector3` or a number. It
  also features methods for common vector operations, such as
  `Datatype.Vector3:Cross()|Cross()` and `Datatype.Vector3:Dot()|Dot()`.

  Alternatively to `Datatype.Vector3`, consider using the methods and properties
  of the `Library.vector` library.

  Some example usages of `Datatype.Vector3` are the
  `Class.BasePart.Position|Position`, `Class.BasePart.Rotation|Rotation`, and
  `Class.BasePart.Size|Size` of parts, for example:

  ```lua
  local part = Instance.new("Part")
  part.Position = part.Position + Vector3.new(5, 2, 10) -- Move part by (5, 2, 10)
  print(part.Position) --> 5, 2, 10
  ```

  `Datatype.Vector3` is also commonly used when constructing more complex 3D
  data types such as `Datatype.CFrame`. Many of these data types' methods will
  use a `Datatype.Vector3` within their parameters, such as
  `Datatype.CFrame:PointToObjectSpace()`.
code_samples: []
tags: []
deprecation_message: ''
constructors:
  - name: Vector3.new
    summary: |
      Returns a new `Datatype.Vector3` from the given `x`, `y`, and `z`
      components.
    description: |
      Returns a new `Datatype.Vector3` using the given `x`, `y`, and `z`
      components.
    parameters:
      - name: x
        type: number
        default: 0
        summary: |
          The x-axis component of the vector.
      - name: 'y'
        type: number
        default: 0
        summary: |
          The y-axis component of the vector.
      - name: z
        type: number
        default: 0
        summary: |
          The z-axis component of the vector.
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.FromNormalId
    summary: |
      Returns a new `Datatype.Vector3` in the given direction.
    description: |
      Returns a unit `Datatype.Vector3` pointing in the direction of the given
      `Enum.NormalId`. For example, `Enum.NormalId.Top` returns `(0, 1, 0)` and
      `Enum.NormalId.Front` returns `(0, 0, -1)`.

      ```lua
      print(Vector3.FromNormalId(Enum.NormalId.Right)) --> 1, 0, 0
      print(Vector3.FromNormalId(Enum.NormalId.Left))  --> -1, 0, 0
      ```
    parameters:
      - name: normal
        type: NormalId
        default:
        summary: |
          The `Enum.NormalId` specifying the face direction to convert into a
          unit vector.
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.FromAxis
    summary: |
      Returns a new `Datatype.Vector3` for the given axis.
    description: |
      Returns a unit `Datatype.Vector3` for the given `Enum.Axis`. The mapping
      is `Enum.Axis.X` to `(1, 0, 0)`, `Enum.Axis.Y` to `(0, 1, 0)`, and
      `Enum.Axis.Z` to `(0, 0, 1)`.

      ```lua
      print(Vector3.FromAxis(Enum.Axis.X)) --> 1, 0, 0
      print(Vector3.FromAxis(Enum.Axis.Z)) --> 0, 0, 1
      ```
    parameters:
      - name: axis
        type: Axis
        default:
        summary: |
          The `Enum.Axis` specifying which coordinate axis to convert into a
          unit vector.
    code_samples: []
    tags: []
    deprecation_message: ''
constants:
  - name: Vector3.zero
    type: Vector3
    summary: |
      A `Datatype.Vector3` with a magnitude of `0`.
    description: |
      A `Datatype.Vector3` with a magnitude of `0`.

      This API member is a **constant**, and must be accessed through the
      `Datatype.Vector3` global as opposed to an individual `Datatype.Vector3`
      object.

      ```lua
      print(Vector3.zero)  --> 0, 0, 0
      ```
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.one
    type: Vector3
    summary: |
      A `Datatype.Vector3` with a value of `1` on every axis.
    description: |
      A `Datatype.Vector3` with a value of `1` on every axis.

      This API member is a **constant**, and must be accessed through the
      `Datatype.Vector3` global as opposed to an individual `Datatype.Vector3`
      object.

      ```lua
      print(Vector3.one)  --> 1, 1, 1
      ```
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.xAxis
    type: Vector3
    summary: |
      A `Datatype.Vector3` with a value of `1` on the **X** axis.
    description: |
      A `Datatype.Vector3` with a value of `1` on the **X** axis.

      This API member is a **constant**, and must be accessed through the
      `Datatype.Vector3` global as opposed to an individual `Datatype.Vector3`
      object.

      ```lua
      print(Vector3.xAxis)  --> 1, 0, 0
      ```
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.yAxis
    type: Vector3
    summary: |
      A `Datatype.Vector3` with a value of `1` on the **Y** axis.
    description: |
      A `Datatype.Vector3` with a value of `1` on the **Y** axis.

      This API member is a **constant**, and must be accessed through the
      `Datatype.Vector3` global as opposed to an individual `Datatype.Vector3`
      object.

      ```lua
      print(Vector3.yAxis)  --> 0, 1, 0
      ```
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.zAxis
    type: Vector3
    summary: |
      A `Datatype.Vector3` with a value of `1` on the **Z** axis.
    description: |
      A `Datatype.Vector3` with a value of `1` on the **Z** axis.

      This API member is a **constant**, and must be accessed through the
      `Datatype.Vector3` global as opposed to an individual `Datatype.Vector3`
      object.

      ```lua
      print(Vector3.zAxis)  --> 0, 0, 1
      ```
    code_samples: []
    tags: []
    deprecation_message: ''
properties:
  - name: Vector3.zero
    type: Vector3
    summary: |
      A `Datatype.Vector3` with a magnitude of `0`.
    description: |
      A `Datatype.Vector3` with a magnitude of `0`.

      This API member is a **constant**, and must be accessed through the
      `Datatype.Vector3` global as opposed to an individual `Datatype.Vector3`
      object.

      ```lua
      print(Vector3.zero) --> 0, 0, 0
      ```
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.one
    type: Vector3
    summary: |
      A `Datatype.Vector3` with a value of `1` on every axis.
    description: |
      A `Datatype.Vector3` with a value of `1` on every axis.

      This API member is a **constant**, and must be accessed through the
      `Datatype.Vector3` global as opposed to an individual `Datatype.Vector3`
      object.

      ```lua
      print(Vector3.one)  --> 1, 1, 1
      ```
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.xAxis
    type: Vector3
    summary: |
      A `Datatype.Vector3` with a value of `1` on the **X** axis.
    description: |
      A `Datatype.Vector3` with a value of `1` on the **X** axis.

      This API member is a **constant**, and must be accessed through the
      `Datatype.Vector3` global as opposed to an individual `Datatype.Vector3`
      object.

      ```lua
      print(Vector3.xAxis)  --> 1, 0, 0
      ```
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.yAxis
    type: Vector3
    summary: |
      A `Datatype.Vector3` with a value of `1 `on the **Y** axis.
    description: |
      A `Datatype.Vector3` with a value of `1` on the **Y** axis.

      This API member is a **constant**, and must be accessed through the
      `Datatype.Vector3` global as opposed to an individual `Datatype.Vector3`
      object.

      ```lua
      print(Vector3.yAxis)  --> 0, 1, 0
      ```
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.zAxis
    type: Vector3
    summary: |
      A `Datatype.Vector3` with a value of `1` on the **Z** axis.
    description: |
      A `Datatype.Vector3` with a value of `1` on the **Z** axis.

      This API member is a **constant**, and must be accessed through the
      `Datatype.Vector3` global as opposed to an individual `Datatype.Vector3`
      object.

      ```lua
      print(Vector3.zAxis)  --> 0, 0, 1
      ```
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.X
    type: number
    summary: |
      The **X** coordinate of the `Datatype.Vector3`.
    description: |
      The **X** coordinate of the `Datatype.Vector3`. In world space, this axis
      corresponds to the left-right (east-west) direction.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.Y
    type: number
    summary: |
      The **Y** coordinate of the `Datatype.Vector3`.
    description: |
      The **Y** coordinate of the `Datatype.Vector3`. In world space, this axis
      corresponds to the up-down (vertical) direction.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.Z
    type: number
    summary: |
      The **Z** coordinate of the `Datatype.Vector3`.
    description: |
      The **Z** coordinate of the `Datatype.Vector3`. In world space, this axis
      corresponds to the north-south (forward-back) direction.
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.Magnitude
    type: number
    summary: |
      The length of the `Datatype.Vector3`.
    description: |
      The length (magnitude) of the `Datatype.Vector3`, computed as
      `math.sqrt(X^2 + Y^2 + Z^2)`. This is useful for comparing distances or
      determining how far a point is from the origin.

      ```lua
      local v = Vector3.new(3, 4, 0)
      print(v.Magnitude) --> 5
      ```
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3.Unit
    type: Vector3
    summary: |
      A normalized copy of the `Datatype.Vector3` - one that has the same
      direction as the original but a magnitude of `1`.
    description: |
      A normalized copy of the `Datatype.Vector3` — one that has the same
      direction as the original but a magnitude of `1`. This is useful when you
      need only the direction of a vector without its length, for example to get
      a movement direction regardless of speed.

      If the vector has a magnitude of `0` (i.e. all components are zero), the
      resulting `Unit` vector will have `NaN` components. Check
      `Datatype.Vector3.Magnitude|Magnitude` before using `Unit` when the vector
      may be zero-length.

      ```lua
      local v = Vector3.new(3, 4, 0)
      print(v.Unit)      --> 0.6, 0.8, 0
      print(v.Unit.Magnitude) --> 1
      ```
    default:
    code_samples: []
    tags: []
    deprecation_message: ''
methods:
  - name: Vector3:Abs
    summary: |
      Returns a new vector from the absolute values of the original's
      components.
    description: |
      Returns a new vector from the absolute values of the original's
      components. For example, a vector of `(-2, 4, -6)` returns a vector of
      `(2, 4, 6)`.
    parameters: []
    returns:
      - type: Vector3
        summary: ''
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3:Ceil
    summary: |
      Returns a new vector from the ceiling of the original's components.
    description: |
      Returns a new vector from the ceiling of the original's components. For
      example, a vector of `(-2.6, 5.1, 8.8)` returns a vector of `(-2, 6, 9)`.
    parameters: []
    returns:
      - type: Vector3
        summary: ''
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3:Floor
    summary: |
      Returns a new vector from the floor of the original's components.
    description: |
      Returns a new vector from the floor of the original's components. For
      example, a vector of `(-2.6, 5.1, 8.8)` returns a vector of `(-3, 5, 8)`.
    parameters: []
    returns:
      - type: Vector3
        summary: ''
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3:Sign
    summary: |
      Returns a new vector from the sign (-1, 0, or 1) of the original's
      components.
    description: |
      Returns a new vector from the sign (-1, 0, or 1) of the original's
      components. For example, a vector of `(-2.6, 5.1, 0)` returns a vector of
      `(-1, 1, 0)`.
    parameters: []
    returns:
      - type: Vector3
        summary: ''
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3:Cross
    summary: |
      Returns the cross product of the two vectors.
    description: |
      Returns the cross product of this vector and `other`. The resulting vector
      is perpendicular to both input vectors and has a magnitude equal to the
      area of the parallelogram they span. The direction follows the right-hand
      rule: if you curl the fingers of your right hand from `self` toward
      `other`, your thumb points in the direction of the result.

      The cross product is commonly used to find normals to surfaces, determine
      the axis of rotation between two directions, and test whether two vectors
      are parallel (cross product of parallel vectors is the zero vector).

      ```lua
      local a = Vector3.new(1, 0, 0)
      local b = Vector3.new(0, 1, 0)
      print(a:Cross(b)) --> 0, 0, 1
      print(b:Cross(a)) --> 0, 0, -1
      ```
    parameters:
      - name: other
        type: Vector3
        default:
        summary: |
          The other `Datatype.Vector3` to compute the cross product with.
    returns:
      - type: Vector3
        summary: ''
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3:Angle
    summary: |
      Returns the angle in radians between the two vectors. If you provide an
      axis, it determines the sign of the angle.
    description: |
      Returns the angle in radians between this vector and `other`. The input
      vectors do not need to be unit vectors. Without an `axis` argument, the
      returned angle is always in the range `[0, math.pi]` (unsigned).

      If you provide an `axis` `Datatype.Vector3`, the returned angle is signed:
      positive when the rotation from `self` to `other` follows the right-hand
      rule around `axis`, and negative otherwise. The signed result is in the
      range `[-math.pi, math.pi]`. The `axis` is used only to determine the
      sign; the plane of rotation is still defined by `self` and `other`.

      ```lua
      local a = Vector3.new(1, 0, 0)
      local b = Vector3.new(0, 1, 0)
      print(a:Angle(b))                         --> 1.5707963... (pi/2)
      print(a:Angle(b, Vector3.new(0, 0, 1)))   --> 1.5707963... (positive, CCW about +Z)
      print(b:Angle(a, Vector3.new(0, 0, 1)))   --> -1.5707963... (negative, CW about +Z)
      ```
    parameters:
      - name: other
        type: Vector3
        default:
        summary: |
          The `Datatype.Vector3` to measure the angle to.
      - name: axis
        type: Vector3
        default: nil
        summary: |
          An optional `Datatype.Vector3` used to determine the sign of the angle
          via the right-hand rule.
    returns:
      - type: number
        summary: ''
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3:Dot
    summary: |
      Returns a scalar dot product of the two vectors.
    description: |
      Returns the scalar dot product of this vector and `other`, computed as
      `self.X * other.X + self.Y * other.Y + self.Z * other.Z`.

      The dot product is useful for determining the relationship between two
      directions: it equals the product of their magnitudes multiplied by the
      cosine of the angle between them. For unit vectors, a result of `1` means
      they point in the same direction, `0` means they are perpendicular, and
      `-1` means they point in opposite directions.

      ```lua
      local a = Vector3.new(1, 0, 0)
      local b = Vector3.new(0, 1, 0)
      print(a:Dot(b)) --> 0

      local c = Vector3.new(1, 2, 3)
      local d = Vector3.new(4, 5, 6)
      print(c:Dot(d)) --> 32
      ```
    parameters:
      - name: other
        type: Vector3
        default:
        summary: |
          The `Datatype.Vector3` to compute the dot product with.
    returns:
      - type: number
        summary: ''
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3:FuzzyEq
    summary: |
      Returns `true` if the difference between the squared magnitude of the two
      vectors is within `epsilon`. `epsilon` is scaled relative to the
      magnitude, rather than an absolute epsilon.
    description: |
      Returns `true` if the two vectors are approximately equal within the
      tolerance defined by `epsilon`. The comparison is performed per-component
      using a hybrid epsilon that scales relative to the magnitude of each
      component, making it suitable for both small and large values. The default
      `epsilon` of `0.00001` (1e-5) works well for most cases.

      ```lua
      local a = Vector3.new(1, 2, 3)
      local b = Vector3.new(1.000001, 2, 3)
      print(a:FuzzyEq(b))        --> true  (within default epsilon)
      print(a:FuzzyEq(b, 1e-8))  --> false (tighter tolerance)
      ```
    parameters:
      - name: other
        type: Vector3
        default:
        summary: |
          The `Datatype.Vector3` to compare against.
      - name: epsilon
        type: number
        default: 0.00001 aka 1e-5
        summary: |
          The tolerance threshold for the comparison, scaled relative to the
          component magnitudes.
    returns:
      - type: bool
        summary: ''
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3:Lerp
    summary: |
      Returns a `Datatype.Vector3` linearly interpolated between this
      `Datatype.Vector3` and the given goal by the given alpha.
    description: |
      Returns a `Datatype.Vector3` linearly interpolated between this
      `Datatype.Vector3` and the given `goal` `Datatype.Vector3` by the fraction
      `alpha`. Note that `alpha` is **not** limited to the range `[0, 1]`.
    parameters:
      - name: goal
        type: Vector3
        default:
        summary: |
          The target `Datatype.Vector3` to interpolate toward.
      - name: alpha
        type: number
        default:
        summary: |
          The interpolation fraction, typically between 0 (returns self) and 1
          (returns goal), but not clamped.
    returns:
      - type: Vector3
        summary: ''
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3:Max
    summary: |
      Returns a `Datatype.Vector3` with each component as the highest among the
      respective components of both provided `Datatype.Vector3` objects.
    description: |
      Returns a `Datatype.Vector3` with each component as the highest among the
      respective components of both provided `Datatype.Vector3` objects.

      ```lua
      local a = Vector3.new(1, 2, 1)
      local b = Vector3.new(2, 1, 2)

      print(a:Max(b))  --> Vector3.new(2, 2, 2)
      ```
    parameters:
      - name: vector
        type: Vector3
        default:
        summary: |
          The `Datatype.Vector3` to compare component-wise against.
    returns:
      - type: Vector3
        summary: ''
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: Vector3:Min
    summary: |
      Returns a `Datatype.Vector3` with each component as the lowest among the
      respective components of both provided `Datatype.Vector3` objects.
    description: |
      Returns a `Datatype.Vector3` with each component as the lowest among the
      respective components of both provided `Datatype.Vector3` objects.

      ```lua
      local a = Vector3.new(1, 2, 1)
      local b = Vector3.new(2, 1, 2)

      print(a:Min(b))  --> Vector3.new(1, 1, 1)
      ```
    parameters:
      - name: vector
        type: Vector3
        default:
        summary: |
          The `Datatype.Vector3` to compare component-wise against.
    returns:
      - type: Vector3
        summary: ''
    code_samples: []
    tags: []
    deprecation_message: ''
functions: []
math_operations:
  - name: +
    operation: +
    summary: |
      Produces a `Datatype.Vector3` by adding each component of the first vector
      to the corresponding component of the second.
    description: |
      Produces a `Datatype.Vector3` by adding each component of the first vector
      to the corresponding component of the second.
    type_a: Vector3
    type_b: Vector3
    return_type: Vector3
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: '-'
    operation: '-'
    summary: |
      Produces a `Datatype.Vector3` by subtracting each component of the second
      vector from the corresponding component of the first.
    description: |
      Produces a `Datatype.Vector3` by subtracting each component of the second
      vector from the corresponding component of the first.
    type_a: Vector3
    type_b: Vector3
    return_type: Vector3
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: '*'
    operation: '*'
    summary: |
      Produces a `Datatype.Vector3` by multiplying each component of the first
      vector by the corresponding component of the second.
    description: |
      Produces a `Datatype.Vector3` by multiplying each component of the first
      vector by the corresponding component of the second.
    type_a: Vector3
    type_b: Vector3
    return_type: Vector3
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: /
    operation: /
    summary: |
      Produces a `Datatype.Vector3` by dividing each component of the first
      vector by the corresponding component of the second.
    description: |
      Produces a `Datatype.Vector3` by dividing each component of the first
      vector by the corresponding component of the second.
    type_a: Vector3
    type_b: Vector3
    return_type: Vector3
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: //
    operation: //
    summary: |
      Produces a `Datatype.Vector3` by **floor dividing** each component of the
      first vector by the corresponding component of the second.
    description: |
      Produces a `Datatype.Vector3` by **floor dividing** each component of the
      first vector by the corresponding component of the second.
    type_a: Vector3
    type_b: Vector3
    return_type: Vector3
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: '*'
    operation: '*'
    summary: |
      Produces a `Datatype.Vector3` by multiplying each component of the
      provided vector by the number.
    description: |
      Produces a `Datatype.Vector3` by multiplying each component of the
      provided vector by the number.
    type_a: Vector3
    type_b: number
    return_type: Vector3
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: /
    operation: /
    summary: |
      Produces a `Datatype.Vector3` by dividing each component of the provided
      vector by the number.
    description: |
      Produces a `Datatype.Vector3` by dividing each component of the provided
      vector by the number.
    type_a: Vector3
    type_b: number
    return_type: Vector3
    code_samples: []
    tags: []
    deprecation_message: ''
  - name: //
    operation: //
    summary: |
      Produces a `Datatype.Vector3` by **floor dividing** each component of the
      provided vector by the number.
    description: |
      Produces a `Datatype.Vector3` by **floor dividing** each component of the
      provided vector by the number.
    type_a: Vector3
    type_b: number
    return_type: Vector3
    code_samples: []
    tags: []
    deprecation_message: ''
