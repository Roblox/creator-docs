# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: Secret
type: datatype
summary: |
  Stores secret non-printable content.
description: |
  The `Datatype.Secret` data type stores the secret content returned by
  `Class.HttpService:GetSecret()`. It cannot be printed or logged, but can be
  modified using built-in functions:
code_samples:
  - Secret-Overview
tags: []
deprecation_message: ''
constructors: []
constants: []
properties: []
methods:
  - name: Secret:AddPrefix
    summary: |
      Prepends a string to the secret content.
    description: |
      Returns a secret formed by concatenating the supplied string to the secret
      content, for example prepending `"Bearer"` to the API key.
    parameters:
      - name: prefix
        type: string
        default:
        summary: |
          The string to prepend to the secret content.
    returns:
      - type: Secret
        summary: |
          A new Secret with the prefix prepended to the original secret value.
    code_samples:
      - Secret-AddPrefix
    tags: []
    deprecation_message: ''
  - name: Secret:AddSuffix
    summary: |
      Appends a string to the secret content.
    description: |
      Returns a secret formed by concatenating the original secret and the
      supplied string parameter. Useful when creating a URL containing a key and
      query parameters.
    parameters:
      - name: suffix
        type: string
        default:
        summary: |
          The string to append to the secret content.
    returns:
      - type: Secret
        summary: |
          A new Secret with the suffix appended to the original secret value.
    code_samples:
      - Secret-AddSuffix
    tags: []
    deprecation_message: ''
functions: []
math_operations: []
