# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: Language
type: enum
summary: |
  An enumeration embedded in `Class.LocalizationService` that specifies a
  language context for localization operations; currently defines only
  `Default`.
description: |
  An enumeration embedded in `Class.LocalizationService` that represents a
  language selector for localization-related operations. The only defined item
  is `Enum.Language.Default`, which instructs the engine to resolve the language
  from the player's system locale rather than from a hard-coded override.
code_samples: []
tags: []
deprecation_message: ''
items:
  - name: Default
    summary: |
      Instructs the engine to use the player's system-determined locale for
      language resolution rather than a fixed override.
    value: 0
    tags: []
    deprecation_message: ''
