# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: vector
type: library
summary: |
  A library of vector functions.
description: |
  This library implements functionality for the vector type in addition to the
  built-in primitive operator support. It uses vectors with three components
  (`x`, `y`, and `z`).

  Individual vector components can be accessed using the fields `x` or `X`, `y`
  or `Y`, `z` or `Z`. Since vector values are immutable, writing to individual
  components is not supported.
tags: []
code_samples: []
properties:
  - name: vector.zero
    type: vector
    summary: |
      Constant vector with all components set to `0`.
    description: |
      A pre-allocated constant equivalent to `vector.create(0, 0, 0)`. Useful as
      a default value or additive identity in vector arithmetic without
      allocating a new vector each time.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.one
    type: vector
    summary: |
      Constant vector with all components set to `1`.
    description: |
      A pre-allocated constant equivalent to `vector.create(1, 1, 1)`. Useful as
      a multiplicative identity or scale factor in vector arithmetic without
      allocating a new vector each time.
    tags: []
    code_samples: []
    deprecation_message: ''
functions:
  - name: vector.create
    summary: |
      Creates a new vector with the given component values.
    description: |
      Creates a new vector with the given component values. The `z` parameter
      defaults to `0` when omitted, so `vector.create(1, 2)` is equivalent to
      `vector.create(1, 2, 0)`.
    parameters:
      - name: x
        type: number
        default:
        summary: |
          The x component of the new vector.
      - name: 'y'
        type: number
        default:
        summary: |
          The y component of the new vector.
      - name: z
        type: number
        default:
        summary: |
          The z component of the new vector. Defaults to `0` when omitted.
    returns:
      - type: vector
        summary: |
          A new vector with the specified x, y, and z component values.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.magnitude
    summary: |
      Calculates the magnitude of a given vector.
    description: |
      Returns the magnitude (Euclidean length) of the vector, computed as
      `math.sqrt(vec.x^2 + vec.y^2 + vec.z^2)`.
    parameters:
      - name: vec
        type: vector
        default:
        summary: |
          The vector whose magnitude is computed.
    returns:
      - type: number
        summary: |
          The Euclidean length of the vector.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.normalize
    summary: |
      Computes the normalized version (unit vector) of a given vector.
    description: |
      Returns a unit vector (magnitude of 1) pointing in the same direction as
      the input. Computed as `vec / vector.magnitude(vec)`. If the input vector
      has zero magnitude, the result contains `nan` components.
    parameters:
      - name: vec
        type: vector
        default:
        summary: |
          The vector to normalize.
    returns:
      - type: vector
        summary: |
          A unit vector pointing in the same direction as the input.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.cross
    summary: |
      Computes the cross product of two vectors.
    description: |
      Returns the cross product of two vectors, which is a vector perpendicular
      to both inputs whose magnitude equals the area of the parallelogram they
      span. The result is computed as
      `vector.create(vec1.y*vec2.z - vec1.z*vec2.y, vec1.z*vec2.x - vec1.x*vec2.z, vec1.x*vec2.y - vec1.y*vec2.x)`.
    parameters:
      - name: vec1
        type: vector
        default:
        summary: |
          The first operand vector.
      - name: vec2
        type: vector
        default:
        summary: |
          The second operand vector.
    returns:
      - type: vector
        summary: |
          A vector perpendicular to both inputs whose magnitude equals the area
          of the parallelogram they span.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.dot
    summary: |
      Computes the dot product of two vectors.
    description: |
      Returns the dot product (scalar product) of two vectors, computed as
      `vec1.x*vec2.x + vec1.y*vec2.y + vec1.z*vec2.z`. The result is a single
      number equal to the product of the magnitudes of the two vectors times the
      cosine of the angle between them.
    parameters:
      - name: vec1
        type: vector
        default:
        summary: |
          The first operand vector.
      - name: vec2
        type: vector
        default:
        summary: |
          The second operand vector.
    returns:
      - type: number
        summary: |
          The scalar dot product of the two vectors.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.angle
    summary: |
      Computes the angle between two vectors in radians.
    description: |
      Computes the angle between two vectors in radians. The axis, if specified,
      is used to determine the sign of the angle.
    parameters:
      - name: vec1
        type: vector
        default:
        summary: |
          The first vector.
      - name: vec2
        type: vector
        default:
        summary: |
          The second vector.
      - name: axis
        type: vector?
        default:
        summary: |
          An optional reference axis used to determine the sign of the returned
          angle.
    returns:
      - type: number
        summary: |
          The angle between the two vectors in radians, signed if an axis is
          provided.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.floor
    summary: |
      Applies `Library.math.floor()` to every component of the input vector.
    description: |
      Returns a new vector with each component rounded down to the nearest
      integer, equivalent to
      `vector.create(math.floor(vec.x), math.floor(vec.y), math.floor(vec.z))`.
    parameters:
      - name: vec
        type: vector
        default:
        summary: |
          The vector whose components are rounded down.
    returns:
      - type: vector
        summary: |
          A new vector with each component rounded down to the nearest integer.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.ceil
    summary: |
      Applies `Library.math.ceil()` to every component of the input vector.
    description: |
      Returns a new vector with each component rounded up to the nearest
      integer, equivalent to
      `vector.create(math.ceil(vec.x), math.ceil(vec.y), math.ceil(vec.z))`.
    parameters:
      - name: vec
        type: vector
        default:
        summary: |
          The vector whose components are rounded up.
    returns:
      - type: vector
        summary: |
          A new vector with each component rounded up to the nearest integer.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.abs
    summary: |
      Applies `Library.math.abs()` to every component of the input vector.
    description: |
      Returns a new vector with each component replaced by its absolute value,
      equivalent to
      `vector.create(math.abs(vec.x), math.abs(vec.y), math.abs(vec.z))`.
    parameters:
      - name: vec
        type: vector
        default:
        summary: |
          The vector whose components are made absolute.
    returns:
      - type: vector
        summary: |
          A new vector with each component replaced by its absolute value.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.sign
    summary: |
      Applies `Library.math.sign()` to every component of the input vector.
    description: |
      Returns a new vector where each component is `1` if the corresponding
      input component is positive, `-1` if negative, or `0` if zero. Equivalent
      to `vector.create(math.sign(vec.x), math.sign(vec.y), math.sign(vec.z))`.
      The behavior for negative zero and `nan` is consistent with `math.sign`.
    parameters:
      - name: vec
        type: vector
        default:
        summary: |
          The vector whose component signs are extracted.
    returns:
      - type: vector
        summary: |
          A new vector where each component is the sign of the corresponding
          input component.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.clamp
    summary: |
      Applies `Library.math.clamp()` to every component of the input vector.
    description: |
      Returns a new vector with each component clamped between the corresponding
      components of `min` and `max`. Each component of `max` must be greater
      than or equal to the corresponding component of `min`; otherwise, an error
      is raised.
    parameters:
      - name: vec
        type: vector
        default:
        summary: |
          The vector whose components are clamped.
      - name: min
        type: vector
        default:
        summary: |
          The vector of per-component minimum bounds.
      - name: max
        type: vector
        default:
        summary: |
          The vector of per-component maximum bounds. Each component must be
          greater than or equal to the corresponding component of `min`.
    returns:
      - type: vector
        summary: |
          A new vector with each component clamped between the corresponding
          components of `min` and `max`.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.lerp
    summary: |
      Returns a vector linearly interpolated between two vectors by a fractional
      alpha.
    description: |
      Returns a vector linearly interpolated between two vectors (`vec1`,
      `vec2`) by the fraction `alpha`. Note that `alpha` is **not** limited to
      the range `[0, 1]`.
    parameters:
      - name: vec1
        type: vector
        default:
        summary: |
          The starting vector (returned when `alpha` is `0`).
      - name: vec2
        type: vector
        default:
        summary: |
          The ending vector (returned when `alpha` is `1`).
      - name: alpha
        type: number
        default:
        summary: |
          The interpolation fraction. Not clamped to `[0, 1]`.
    returns:
      - type: vector
        summary: |
          A vector interpolated component-wise between `vec1` and `vec2`.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.max
    summary: |
      Applies `Library.math.max()` to the corresponding components of the input
      vectors.
    description: |
      Returns a new vector where each component is the maximum of the
      corresponding components across all input vectors. Accepts a variadic
      number of vectors (at least one). When a component is `nan` in one vector
      but a valid number in another, the valid number is used.
    parameters:
      - name: ...
        type: vector
        default:
        summary: |
          One or more vectors to compare component-wise.
    returns:
      - type: vector
        summary: |
          A new vector where each component is the maximum of the corresponding
          components across all inputs.
    tags: []
    code_samples: []
    deprecation_message: ''
  - name: vector.min
    summary: |
      Applies `Library.math.min()` to the corresponding components of the input
      vectors.
    description: |
      Returns a new vector where each component is the minimum of the
      corresponding components across all input vectors. Accepts a variadic
      number of vectors (at least one). When a component is `nan` in one vector
      but a valid number in another, the valid number is used.
    parameters:
      - name: ...
        type: vector
        default:
        summary: |
          One or more vectors to compare component-wise.
    returns:
      - type: vector
        summary: |
          A new vector where each component is the minimum of the corresponding
          components across all inputs.
    tags: []
    code_samples: []
    deprecation_message: ''
