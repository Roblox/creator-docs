# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: AnimationNodeDefinition
type: class
memory_category: Instances
summary: |
  A single node within an animation graph, connected to other nodes through
  named input pins.
description: |
  Represents one node in an animation graph, the node-based system that combines
  animation clips and poses at runtime. Its
  `Class.AnimationNodeDefinition.NodeType|NodeType` determines how the node
  processes animation, such as playing a clip, blending or overlaying two
  inputs, masking, or adjusting playback speed, while its
  `Class.AnimationNodeDefinition.NodeId|NodeId` uniquely identifies it within
  the graph. Nodes connect to one another through named input pins; changing a
  node's type resets its input pins to the default set for that type.
code_samples: []
inherits:
  - Instance
descendants: []
tags: []
deprecation_message: ''
properties:
  - name: AnimationNodeDefinition.NodeId
    summary: |
      A string that uniquely identifies this node within its animation graph.
    description: |
      A unique identifier used to reference this node among the other nodes in
      its graph. Defaults to an empty string.
    code_samples: []
    type: string
    tags: []
    deprecation_message: ''
    security:
      read: RobloxScriptSecurity
      write: RobloxScriptSecurity
    thread_safety: ReadSafe
    category: Data
    serialization:
      can_load: true
      can_save: true
    capabilities:
      - Animation
  - name: AnimationNodeDefinition.NodeType
    summary: |
      The kind of node this definition represents within its animation graph.
    description: |
      Determines how the node processes animation within the graph, for example
      a clip source, an additive or overlay blend, a mask, or a speed adjustment
      (see `Enum.AnimationNodeType`). Defaults to
      `Enum.AnimationNodeType|InvalidNode`. Changing this value resets the
      node's input pins to the default set defined for the new type.
    code_samples: []
    type: AnimationNodeType
    tags: []
    deprecation_message: ''
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Animation
    serialization:
      can_load: true
      can_save: true
    capabilities:
      - Animation
methods:
  - name: AnimationNodeDefinition:AddInputPin
    summary: |
      Appends a named input pin to the end of the ordered input pin list.
    description: |
      Appends a named input pin to the end of the ordered input pin list. The
      order in which pins are added is significant for nodes such as
      `Enum.AnimationNodeType.SequenceNode`,
      `Enum.AnimationNodeType.RandomSequenceNode`, and
      `Enum.AnimationNodeType.PrioritySelectNode`.
    code_samples: []
    parameters:
      - name: pin
        type: string
        default:
        summary: |
          Name of the input pin to append. Should match the
          `Class.ObjectValue.Name|Name` of the corresponding `Class.ObjectValue`
          wiring child.
    returns:
      - type: ()
        summary: ''
    tags: []
    deprecation_message: ''
    security: None
    thread_safety: Unsafe
    capabilities:
      - Animation
  - name: AnimationNodeDefinition:GetOrderedInputPinNames
    summary: |
      Returns the current ordered list of input pin names.
    description: |
      Returns the current ordered list of input pin names.
    code_samples: []
    parameters: []
    returns:
      - type: Array
        summary: |
          The ordered list of input pin names.
    tags: []
    deprecation_message: ''
    security: None
    thread_safety: Unsafe
    capabilities:
      - Animation
  - name: AnimationNodeDefinition:RemoveInputPin
    summary: |
      Removes the first input pin matching the given name from the ordered pin
      list.
    description: |
      Removes the first input pin matching the given name from the ordered pin
      list.
    code_samples: []
    parameters:
      - name: pin
        type: string
        default:
        summary: |
          Name of the input pin to remove. Only the first pin matching this name
          is removed.
    returns:
      - type: ()
        summary: ''
    tags: []
    deprecation_message: ''
    security: None
    thread_safety: Unsafe
    capabilities:
      - Animation
  - name: AnimationNodeDefinition:SetOrderedInputPinNames
    summary: |
      Bulk-replaces the entire ordered input pin list.
    description: |
      Bulk-replaces the entire ordered input pin list. Use this to reorder pins
      or apply batch updates in a single call.
    code_samples: []
    parameters:
      - name: pins
        type: Array
        default:
        summary: |
          The new ordered list of input pin names, replacing the existing list.
    returns:
      - type: ()
        summary: ''
    tags: []
    deprecation_message: ''
    security: None
    thread_safety: Unsafe
    capabilities:
      - Animation
events:
  - name: AnimationNodeDefinition.InputPinsChanged
    summary: |
      Fires whenever the ordered input pin list is modified.
    description: |
      Fires whenever the ordered input pin list is modified.
    code_samples: []
    parameters: []
    tags: []
    deprecation_message: ''
    security: None
    capabilities:
      - Animation
callbacks: []
