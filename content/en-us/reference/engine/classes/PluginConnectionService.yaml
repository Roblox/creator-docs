# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: PluginConnectionService
type: class
memory_category: Instances
summary: |
  This service is used by plugins to communicate with other instances of
  themselves running in other data models.
description: |
  This service is used by plugins to communicate with other instances of
  themselves running in other data models (e.g. a plugin in the Studio edit data
  model can communicate with the instance of that same plugin in the Studio
  playtest Server data model). Plugins can retrieve `Class.PluginConnection`
  objects from this service, which provide an API for sending messages across
  the data model boundary. All access to this class is restricted to Plugin
  security. The command bar is not currently supported.
code_samples:
  - plugin-connection-echo
inherits:
  - Instance
descendants: []
tags:
  - NotCreatable
  - Service
  - NotReplicated
deprecation_message: ''
properties: []
methods:
  - name: PluginConnectionService:CanHaveConnectionType
    summary: |
      Checks if the current data model context can ever have connections of a
      given type.
    description: |
      Checks if the current data model can potentially have connections of a
      given `Enum.PluginConnectionTargetType`. Note that it is possible for this
      function to return true even if no such connections currently exist.
    code_samples: []
    parameters:
      - name: type
        type: PluginConnectionTargetType
        default:
        summary: |
          The connection type to check.
    returns:
      - type: boolean
        summary: ''
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: PluginConnectionService:GetPluginConnectionsOfType
    summary: |
      Returns a list of currently connected `Class.PluginConnection` objects
      with the given connection type.
    description: |
      Returns a list of currently connected `Class.PluginConnection` objects
      with the given connection type. Each `PluginConnection` from this list
      will initially have their `Connected` property set to true and correspond
      to a connected data model related to the current data model according to
      their `Enum.PluginConnectionTargetType`. For example, an edit data model
      may have `Enum.PluginConnectionTargetType.Test` connections to communicate
      with its ongoing playtest data models, and each test data model will have
      an `Enum.PluginConnectionTargetType.Edit` connection back to its edit data
      model.
    code_samples: []
    parameters:
      - name: type
        type: PluginConnectionTargetType
        default:
        summary: |
          The connection type to check.
    returns:
      - type: Array
        summary: |
          An array of the currently connected `Class.PluginConnection` objects
          with that target type.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
events:
  - name: PluginConnectionService.Connected
    summary: |
      Fires just after a new `Class.PluginConnection` successfully connects.
    description: |
      Fires just after a new `Class.PluginConnection` successfully connects.
      Plugins may listen for this signal to learn when a new data model that
      they can communicate with becomes available.

      Note: On startup, it is possible some
      `Class.PluginConnection|PluginConnections` already exist. Plugins should
      query `Class.PluginConnectionService:GetPluginConnectionsOfType()` for
      each type they are interested in after connecting to this function.
    code_samples: []
    parameters:
      - name: conn
        type: PluginConnection
        default:
        summary: |
          The newly connected `Class.PluginConnection`.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    capabilities: []
callbacks: []
