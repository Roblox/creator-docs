# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: BasePlayerGui
type: class
memory_category: Instances
summary: |
  `BasePlayerGui` is an abstract class which the GUI drawing storage classes
  inherit from.
description: |
  `BasePlayerGui` is an abstract class that all GUI drawing storage classes
  inherit from.
code_samples: []
inherits:
  - Instance
descendants:
  - CoreGui
  - PlayerGui
  - StarterGui
tags:
  - NotCreatable
deprecation_message: ''
properties: []
methods:
  - name: BasePlayerGui:GetGuiObjectsAtPosition
    summary: |
      Returns a list of all `Class.GuiObject` instances occupying the given
      point on the screen.
    description: |
      Takes a screen position and returns a list of all the `Class.GuiObject`
      instances which are occupying that screen position, sorted in the order
      they appear on screen from top to bottom as the first and last index,
      respectively. The main use case is to get GUI objects under the player's
      mouse or touch inputs to allow for selection or highlighting.

      Since the child classes of `Class.BasePlayerGui` inherit this function, it
      can be fired by container classes such as `Class.PlayerGui` and
      `Class.StarterGui`.
    code_samples: []
    parameters:
      - name: x
        type: int
        default:
        summary: |
          The **X** position on the screen relative to the top-left corner after
          the GUI inset is applied (see `Class.GuiService:GetGuiInset()`).
      - name: 'y'
        type: int
        default:
        summary: |
          The **Y** position on the screen relative to the top-left corner after
          the GUI inset is applied (see `Class.GuiService:GetGuiInset()`).
    returns:
      - type: Instances
        summary: |
          A table of the `Class.GuiObject` instances that occupy the given
          screen space.
    tags: []
    deprecation_message: ''
    security: None
    thread_safety: Unsafe
    capabilities:
      - UI
events: []
callbacks: []
