# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: PlayerGui
type: class
memory_category: Instances
summary: |
  A container that holds a player's UI.
description: |
  The `PlayerGui` container stores objects that create the player's GUI. If a
  `Class.ScreenGui` is a descendant of `PlayerGui`, any `Class.GuiObject` inside
  that `Class.ScreenGui` displays on the player's screen. Any
  `Class.LocalScript` will also run if it is inserted into a `PlayerGui`.

  When a player first joins the experience, their `PlayerGui` is automatically
  inserted into their `Class.Player` object. When the player's
  `Class.Player.Character` spawns for the first time, all of the contents of
  `Class.StarterGui` are automatically copied into the player's `PlayerGui`. If
  `Class.StarterGui.ResetPlayerGuiOnSpawn` is set to `true`, all the contents of
  a player's `PlayerGui` are cleared and replaced with the contents of
  `Class.StarterGui` every time the player's character respawns.

  Note that if `Class.Players.CharacterAutoLoads` is set to `false`, the
  character won't spawn and `Class.StarterGui` contents won't copy over until
  `Class.Player:LoadCharacterAsync()` is called.

  If you need to control a player's UI container during playtime, for example to
  show/hide a specific `Class.ScreenGui` or any of its children, access it as
  follows from a `Class.LocalScript`:
code_samples:
  - PlayerGui-Overview
inherits:
  - BasePlayerGui
descendants: []
tags:
  - NotCreatable
  - PlayerReplicated
deprecation_message: ''
properties:
  - name: PlayerGui.CurrentScreenOrientation
    summary: |
      Describes the player's current screen orientation.
    description: |
      Describes the player's current screen orientation as an
      `Enum.ScreenOrientation` value. Unlike the writable
      `Class.PlayerGui.ScreenOrientation` property, which requests a _preferred_
      orientation mode, `Class.PlayerGui.CurrentScreenOrientation` is read-only
      and reports the orientation the device is actually displaying, updating as
      the player physically rotates a mobile device.
    code_samples: []
    type: ScreenOrientation
    tags:
      - ReadOnly
      - NotReplicated
    deprecation_message: ''
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Data
    serialization:
      can_load: false
      can_save: false
    capabilities:
      - UI
  - name: PlayerGui.ScreenOrientation
    summary: |
      Sets the preferred screen orientation mode for this player, if on a mobile
      device.
    description: |
      Sets the preferred screen orientation mode for this player when on a
      mobile device, expressed as an `Enum.ScreenOrientation` value. Changing it
      requests that the device lock to, or follow, a particular orientation (for
      example `Enum.ScreenOrientation|Portrait` or
      `Enum.ScreenOrientation|LandscapeSensor`). The orientation the device is
      actually displaying is reported separately by the read-only
      `Class.PlayerGui.CurrentScreenOrientation` property. This property
      defaults to `Enum.ScreenOrientation|LandscapeSensor`.
    code_samples: []
    type: ScreenOrientation
    tags: []
    deprecation_message: ''
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Data
    serialization:
      can_load: true
      can_save: true
    capabilities:
      - UI
  - name: PlayerGui.SelectionImageObject
    summary: |
      Overrides the default selection adornment used for gamepads.
    description: |
      Overrides the default selection adornment used for gamepads. For best
      results, this should point to a `Class.GuiObject`.
    code_samples: []
    type: GuiObject
    tags: []
    deprecation_message: ''
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Appearance
    serialization:
      can_load: true
      can_save: true
    capabilities:
      - UI
methods:
  - name: PlayerGui:GetTopbarTransparency
    summary: |
      Returns the transparency of the Topbar.
    description: |
      Returns the current transparency of the topbar `Class.CoreGui`, a value in
      the range `[0, 1]` where `0` is completely opaque and `1` is completely
      transparent. This is the value previously assigned with
      `Class.PlayerGui:SetTopbarTransparency()`, and it defaults to `0.5`. This
      method can only be called from a `Class.LocalScript`; calling it from a
      server script raises an error.
    code_samples:
      - PlayerGui-GetTopbarTransparency1
    parameters: []
    returns:
      - type: float
        summary: |
          The current topbar transparency, a number between 0 (completely
          opaque) and 1 (completely transparent).
    tags:
      - Deprecated
    deprecation_message: ''
    security: None
    thread_safety: Unsafe
    capabilities:
      - UI
  - name: PlayerGui:SetTopbarTransparency
    summary: |
      Sets the transparency of the top bar.
    description: |
      This method sets the transparency of the top bar `Class.CoreGui`. A value
      of `0` is completely opaque and a value of `1` is completely transparent.
      Values outside of the range `[0, 1]` are clamped. The default transparency
      of the topbar is `0.5`.

      Using the `Class.StarterGui:SetCore()` method with the `"TopbarEnabled"`
      option allows you to enable/disable the entire topbar and all of its
      features (player list, health, etc). By contrast, this method only affects
      how the top bar is displayed.
    code_samples:
      - PlayerGui-SetTopbarTransparency1
      - custom-topbar-style
    parameters:
      - name: transparency
        type: float
        default:
        summary: |
          A number between 0 (completely opaque) and 1 (completely transparent).
          Values outside this range are clamped.
    returns:
      - type: ()
        summary: ''
    tags:
      - Deprecated
    deprecation_message: ''
    security: None
    thread_safety: Unsafe
    capabilities:
      - UI
events:
  - name: PlayerGui.TopbarTransparencyChangedSignal
    summary: |
      Fires when the transparency of the Topbar CoreGui changes.
    description: |
      Fires whenever the transparency of the topbar `Class.CoreGui` changes,
      such as after a call to `Class.PlayerGui:SetTopbarTransparency()`. The
      event passes the new transparency as its `transparency` argument, a value
      in the range `[0, 1]`. It only fires when the clamped transparency differs
      from the previous value, so setting the same value again does not re-fire
      it.
    code_samples:
      - PlayerGui-TopbarTransparencyChangedSignal1
    parameters:
      - name: transparency
        type: float
        default:
        summary: |
          The new topbar transparency after the change, a number between 0
          (completely opaque) and 1 (completely transparent).
    tags:
      - Deprecated
    deprecation_message: ''
    security: None
    capabilities:
      - UI
callbacks: []
