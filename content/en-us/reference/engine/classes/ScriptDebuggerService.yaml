# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: ScriptDebuggerService
type: class
memory_category: Instances
summary: |
  Provides programmatic breakpoint management, execution control, and runtime
  inspection of Luau scripts during a playtest.
description: |
  `ScriptDebuggerService` exposes the Roblox Studio Luau debugger for
  programmatic use. It provides functionality for breakpoint management,
  execution control, and runtime state inspection.

  **APIs in this class are currently in beta and are subject to breaking
  changes.**

  #### Breakpoint Propagation

  Breakpoints set in the **edit** `Class.DataModel` are set on the specific
  script instance and do not propagate to clones, but they propagate to
  corresponding scripts in play data models at the start of a playtest.
  Breakpoints set in a **play** `Class.DataModel` propagate to script clones in
  the same data model and to corresponding scripts in other data models.

  #### Parallel Threads

  The behavior of this API with parallel Luau is undefined.
code_samples: []
inherits:
  - Instance
descendants: []
tags:
  - NotCreatable
  - Service
  - NotReplicated
deprecation_message: ''
properties: []
methods:
  - name: ScriptDebuggerService:AddBreakpoint
    summary: |
      Adds a breakpoint to a script. If a breakpoint already exists on the same
      script and line, its data is replaced.
    description: |
      Adds a breakpoint to the specified script. If a breakpoint already exists
      on the same script and line, its data is replaced with the new
      configuration.

      Errors if the script instance or breakpoint argument is invalid.
    code_samples: []
    parameters:
      - name: scriptInstance
        type: LuaSourceContainer
        default:
        summary: |
          The `Class.LuaSourceContainer` to place the breakpoint on.
      - name: breakpoint
        type: Dictionary
        default:
        summary: |
          Dictionary describing the breakpoint configuration through the
          following key-value pairs:

          - `Line` — Required 1-based line number.
          - `Enabled` — Optional boolean whether the breakpoint is active.
            Default is `true`.
          - `Condition` — Optional string indicating the Luau expression which
            must be truthy to pause, for example `"health < 10"`.
          - `LogMessage` — Optional string message logged when the breakpoint is
            hit. This string is parsed as a comma-separated list of Luau
            expressions, evaluated in the breakpoint's scope, and concatenated
            `Global.LuaGlobals.print()`‑style with spaces between segments.
            String literals are quoted; bare identifiers reference live values.
            For example, `"'count is', count"` produces output like
            `count is 7`.
          - `ContinueExecution` — If `true`, the `Class.DataModel` does not
            pause when the breakpoint is hit. Default is `false`.
    returns:
      - type: ScriptBreakpointResult
        summary: |
          Dictionary indicating whether the breakpoint was placed successfully
          and on which line. Includes the following key-value pairs:

          - `Verified` — Boolean value indicating whether the breakpoint was
            placed successfully.
          - `Line` — The line number the breakpoint was placed on.
          - `Message` — Optional explanation if `Verified` is `false`.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDebuggerService:RemoveBreakpoint
    summary: |
      Removes the breakpoint on the given script and line.
    description: |
      Removes the breakpoint at the specified line in the given script. Returns
      `false` if no breakpoint exists on the line (no-op). Errors if the script
      instance or line number is invalid.
    code_samples: []
    parameters:
      - name: scriptInstance
        type: LuaSourceContainer
        default:
        summary: |
          The `Class.LuaSourceContainer` containing the breakpoint.
      - name: line
        type: int
        default:
        summary: |
          The 1-based line number of the breakpoint to remove.
    returns:
      - type: boolean
        summary: |
          `true` if a breakpoint was removed, `false` if no breakpoint existed
          on the line.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDebuggerService:ClearBreakpoints
    summary: |
      Removes all breakpoints across all scripts.
    description: |
      Removes all breakpoints across all scripts.
    code_samples: []
    parameters: []
    returns:
      - type: ()
        summary: ''
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDebuggerService:SetExceptionBreakMode
    summary: |
      Controls when the debugger pauses on exceptions.
    description: |
      Sets the exception break mode on **all** `Class.DataModel|DataModels`. Use
      `Enum.DebugBreakModeType` to specify when the debugger should pause on
      exceptions.
    code_samples: []
    parameters:
      - name: breakMode
        type: DebugBreakModeType
        default:
        summary: |
          The `Enum.DebugBreakModeType` to set.
    returns:
      - type: ()
        summary: ''
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDebuggerService:Pause
    summary: |
      Requests the debugger to pause at the next safe point.
    description: |
      Requests the debugger to pause at the next safe point. This method is
      asynchronous and returns immediately. When the thread pauses,
      `Class.ScriptDebuggerService.OnStopped|OnStopped` fires with reason
      `Enum.ScriptStoppedReason.Pause`. Has no effect if already stopped.

      Only meaningful when the `Class.DataModel` is running during a playtest.
      Calling `Pause()` while already stopped at a breakpoint has no effect.
    code_samples: []
    parameters: []
    returns:
      - type: ()
        summary: ''
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDebuggerService:GetThreads
    summary: |
      Returns all paused Luau threads.
    description: |
      Returns all paused Luau threads. Should be called when the
      `Class.DataModel` is stopped (typically inside
      `Class.ScriptDebuggerService.OnStopped|OnStopped`). Returns empty results
      when the `Class.DataModel` is not stopped at a breakpoint or exception, or
      when stopped via `Class.ScriptDebuggerService:Pause()|Pause()`.
    code_samples: []
    parameters: []
    returns:
      - type: List<ScriptDebugThread>
        summary: |
          An array of script debug thread dictionaries, each containing the
          following key-value pairs:

          - `Id` — Numerical thread identifier; use with
            `Class.ScriptDebuggerService:GetStackTrace()|GetStackTrace()` and
            stepping.
          - `Name` — Human-readable name of the script.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDebuggerService:GetStackTrace
    summary: |
      Returns the call stack for a paused thread.
    description: |
      Returns the call stack for a paused thread, ordered innermost (current
      execution point) to outermost. Use `startFrame` (1‑based) for paginated
      retrieval of large stacks.

      Errors if `threadId` or `startFrame` is invalid.
    code_samples: []
    parameters:
      - name: threadId
        type: int
        default:
        summary: |
          The thread identifier from a script debug thread `Id` field (see
          `Class.ScriptDebuggerService:GetThreads()|GetThreads()`).
      - name: startFrame
        type: int?
        default: nil
        summary: |
          Optional 1-based frame index for paginated retrieval.
    returns:
      - type: DebugStackTraceResult
        summary: |
          Dictionary containing the frames ordered innermost (current) to
          outermost. Contains the following key-value pairs:

          - `Frames` — Array of debug stack frame dictionaries. Each dictionary
            item contains the following key-value pairs:
            - `Id` — Numerical frame identifier; use with
              `Class.ScriptDebuggerService:GetRootVariables()|GetRootVariables()`
              and `Class.ScriptDebuggerService:Evaluate()|Evaluate()`.
            - `Name` — Human-readable name of the function at this frame.
            - `ScriptPath` — Full instance path of the script, for example
              `"ServerScriptService.MainScript"`.
            - `Line` — 1-based line number where execution is paused at this
              frame.
          - `TotalFrames` — Total frame count, provided when paginating.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDebuggerService:GetRootVariables
    summary: |
      Returns the root variables (locals, upvalues, globals) for a stack frame.
    description: |
      Returns the root variables (locals, upvalues, globals) for the specified
      stack frame. Each variable includes a `VariablesReference` field; if
      greater than `0`, pass it to
      `Class.ScriptDebuggerService:GetVariables()|GetVariables()` to drill into
      children.

      Errors if `frameId` is invalid. Returns empty if the `Class.DataModel` is
      not stopped at a breakpoint or exception, or when stopped via
      `Class.ScriptDebuggerService:Pause()|Pause()`.
    code_samples: []
    parameters:
      - name: frameId
        type: int
        default:
        summary: |
          The frame identifier from a debug stack frame `Id` field (see
          `Class.ScriptDebuggerService:GetStackTrace()|GetStackTrace()`).
    returns:
      - type: List<ScriptVariable>
        summary: |
          An array of script variable dictionaries, each containing the
          following key-value pairs:

          - `Name` — String value indicating the variable name or table key.
          - `Value` — String representation of the value.
          - `Type` — String indicating the Luau type (`"number"`, `"string"`,
            `"table"`, `"Instance"`, etc.).
          - `Scope` — `Enum.ScriptVariableScope` value (children inherit
            parent's scope).
          - `VariablesReference` — If greater than `0`, call
            `Class.ScriptDebuggerService:GetVariables()|GetVariables()` with
            this to get children.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDebuggerService:GetVariables
    summary: |
      Drills into structured variables (tables, `Class.Instance|Instances`).
    description: |
      Drills into structured variables such as tables and
      `Class.Instance|Instances`. Pass a `VariablesReference` obtained from a
      script variable returned by
      `Class.ScriptDebuggerService:GetRootVariables()|GetRootVariables()` or a
      previous call to this method.
    code_samples: []
    parameters:
      - name: variablesReference
        type: int
        default:
        summary: |
          A reference from a previous script variable's `VariablesReference`
          field (see
          `Class.ScriptDebuggerService:GetRootVariables()|GetRootVariables()`).
    returns:
      - type: List<ScriptVariable>
        summary: |
          An array of script variable dictionaries representing the children in
          the same format as variable dictionaries from
          `Class.ScriptDebuggerService:GetRootVariables()|GetRootVariables()`.
          Returns empty if the `Class.DataModel` is not stopped at a breakpoint
          or exception, or when stopped via
          `Class.ScriptDebuggerService:Pause()|Pause()`.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDebuggerService:Evaluate
    summary: |
      Evaluates a Luau expression in a stack frame's context.
    description: |
      Evaluates a Luau expression in the context of the specified stack frame,
      or globally if no `frameId` is provided.

      Errors if the expression has a syntax error or `frameId` is invalid.
    code_samples: []
    parameters:
      - name: expression
        type: string
        default:
        summary: |
          The Luau expression to evaluate.
      - name: frameId
        type: int?
        default: nil
        summary: |
          Optional frame identifier. If omitted, evaluates globally.
    returns:
      - type: ScriptEvaluateResult
        summary: |
          Dictionary with the following key-value pairs:

          - `Result` — String representation of the evaluated result.
          - `Type` — String indicating the Luau type of the result (`"number"`,
            `"string"`, `"table"`, `"Instance"`, etc.).
          - `VariablesReference` — If greater than `0`, drill into with
            `Class.ScriptDebuggerService:GetVariables()|GetVariables()`.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
events:
  - name: ScriptDebuggerService.Resumed
    summary: |
      Fires when a previously paused thread resumes execution.
    description: |
      Fires when a previously paused thread resumes. After this event, all
      `frameId` values, `VariablesReference` values, and script variable objects
      from that thread are invalidated. Re-fetch them the next time the
      `Class.DataModel` stops if needed.

      Avoid modifying the `Class.DataModel`, throwing unhandled errors, or
      calling async yielding functions inside this event handler.
    code_samples: []
    parameters:
      - name: threadIds
        type: Array
        default:
        summary: |
          An array of thread identifiers that resumed.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    capabilities: []
callbacks:
  - name: ScriptDebuggerService.OnStopped
    summary: |
      The primary callback for reacting to debugger pauses. Returns a resume
      action.
    description: |
      The primary mechanism for reacting to debugger pauses. Set this to a
      function that receives a `stopped` payload and returns a script resume
      action dictionary indicating how execution should continue.

      Only one `OnStopped` per `Class.DataModel` is allowed; it is not inherited
      from the edit `Class.DataModel`.

      If the callback returns nothing or throws,
      `Enum.DebuggerResumeType.Resume` is assumed. Avoid modifying the
      `Class.DataModel`, throwing unhandled errors, or calling async yielding
      functions inside this callback.

      #### Late-Set Behavior

      If `OnStopped` is set while the `Class.DataModel` is already stopped at a
      breakpoint and the previous value was `nil`, the callback runs
      immediately.
    code_samples: []
    parameters:
      - name: stopped
        type: Dictionary
        default:
        summary: |
          Dictionary describing why the debugger paused. The following key-value
          pairs are valid:

          - `Reason` — `Enum.ScriptStoppedReason` why the debugger paused.
          - `ThreadIds` — Array of thread identifiers that stopped.
          - `ExceptionText` — Error message present when `Reason` is
            `Enum.ScriptStoppedReason.Exception`.
    returns:
      - type: ScriptResumeAction
        summary: |
          Dictionary specifying how to resume execution. Contains the following
          key-value pairs:

          - `steppedType` — `Enum.DebuggerResumeType` describing how to resume.
          - `threadId` — Number indicating which thread to step. Required for
            step actions.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
    writeCapabilities: []
