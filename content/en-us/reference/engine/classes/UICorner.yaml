# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: UICorner
type: class
memory_category: Instances
summary: |
  UI modifier which applies deformation to corners of its parent
  `Class.GuiObject`.
description: |
  `Class.UICorner` is a modifier which applies deformation to corners of its
  parent `Class.GuiObject`. Input, but not descendants, will be clipped to the
  round corner area. See [here](../../../ui/appearance-modifiers.md#corners) for
  examples.

  Each corner has its own radius property
  (`Class.UICorner.TopLeftRadius|TopLeftRadius`,
  `Class.UICorner.TopRightRadius|TopRightRadius`,
  `Class.UICorner.BottomRightRadius|BottomRightRadius`,
  `Class.UICorner.BottomLeftRadius|BottomLeftRadius`). The
  `Class.UICorner.CornerRadius|CornerRadius` property is a convenience shorthand
  that sets all four corners at once and reads from
  `Class.UICorner.TopLeftRadius|TopLeftRadius`.

  In order to keep the circular shape of round corners, each corner radius
  (`Datatype.UDim`) is internally calculated as follows:

  - The radius of the **X** axis is always the same as the radius of **Y** axis.
  - `Datatype.UDim.Scale|Scale` rounding will always apply to the **minimum**
    width or height.
  - Rounded rectangles will always be in a "pill" shape if
    `Class.UICorner.CornerRadius|CornerRadius` is set to a value that leads to a
    calculated result greater than half of the rectangle's minimum width or
    height.

  Alternatively, rounded corners can be accomplished using **slices** which are
  suitable for decorative borders that are not simply rounded. See
  [9‑Slice Design](../../../ui/9-slice.md) for details.

  When using [styling](../../../ui/styling/index.md) with `Class.UICorner`,
  avoid configuring both `Class.UICorner.CornerRadius|CornerRadius` and the
  individual corner radii in style rules at the same time, as this may produce
  unexpected results.

  Note that `Class.UICorner` can not be applied to a `Class.ScrollingFrame`.
code_samples: []
inherits:
  - UIComponent
descendants: []
tags: []
deprecation_message: ''
properties:
  - name: UICorner.CornerRadius
    summary: |
      Sets all four corner radii at once and reads from
      `Class.UICorner.TopLeftRadius|TopLeftRadius`.
    description: |
      A convenience `Datatype.UDim` property that acts as a shorthand for the
      four individual corner radius properties. Writing to this property sets
      `Class.UICorner.TopLeftRadius|TopLeftRadius`,
      `Class.UICorner.TopRightRadius|TopRightRadius`,
      `Class.UICorner.BottomRightRadius|BottomRightRadius`, and
      `Class.UICorner.BottomLeftRadius|BottomLeftRadius` to the same value.
      Reading this property returns the value of
      `Class.UICorner.TopLeftRadius|TopLeftRadius`.
    code_samples: []
    type: UDim
    tags: []
    deprecation_message: ''
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Appearance
    serialization:
      can_load: true
      can_save: false
    capabilities:
      - UI
methods: []
events: []
callbacks: []
