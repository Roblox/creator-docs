# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: WorldModel
type: class
memory_category: BaseParts
summary: |
  Extends limited physics to its children.
description: |
  When parented to a `Class.ViewportFrame`, `WorldModel` extends limited physics
  for its children on to that `Class.ViewportFrame`. For example,
  `Class.BasePart|BaseParts` that are parented to the `WorldModel` can be
  animated and spatially queried (for example by raycasts) but those parts are
  **not** simulated. Furthermore, you can put `Class.Humanoid` characters in the
  `WorldModel` and their joints will be set up correctly for animation.

  `WorldModel` also exposes the
  `Class.WorldModel.UseWorkspaceCollisionGroups|UseWorkspaceCollisionGroups`
  property to determine whether it uses `Class.Workspace` collision groups or
  its own independent collision groups for queries.

  To avoid possible performance issues, make sure to only create `WorldModels`
  when you want to show them and to delete `WorldModels` that are currently not
  in use.
code_samples: []
inherits:
  - WorldRoot
descendants: []
tags: []
deprecation_message: ''
properties:
  - name: WorldModel.UseWorkspaceCollisionGroups
    summary: |
      Determines whether this `WorldModel` uses `Class.Workspace` collision
      groups or its own independent collision groups for queries.
    description: |
      When `false` (default), this `WorldModel` uses its own collision groups
      for all collision-related behavior.

      When `true`, this `WorldModel` uses `Class.Workspace` collision groups.
      Additionally, the collision group state of this `WorldModel` can continue
      to be modified.
    code_samples: []
    type: boolean
    tags: []
    deprecation_message: ''
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Behavior
    serialization:
      can_load: true
      can_save: true
    capabilities:
      - Basic
methods: []
events: []
callbacks: []
