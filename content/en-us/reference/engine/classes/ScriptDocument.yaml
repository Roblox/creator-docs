# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: ScriptDocument
type: class
memory_category: Instances
summary: |
  Represents the ephemeral state of an open document in the Studio Script
  Editor, providing APIs for reading and editing code.
description: |
  A `Class.ScriptDocument` instance is a proxy of the document of a Studio
  Script Editor. It's different from the `Class.LuaSourceContainer` open in the
  editor in that it represents the ephemeral state of an open document, and its
  representation is in a format that's more suited for reading and editing code
  than executing it. In particular, `Class.ScriptDocument` reflects any changes
  that have been made to the open script in Drafts Mode, which the source
  property doesn't.

  The Script Editor itself exists and changes on a different thread than any
  `Class.DataModel`, so the `Class.ScriptDocument` replicates the open Script
  Editor, but it isn't the open editor. Because of the replication, there's
  sometimes a slight delay between changing the text in the editor and updating
  the `Class.ScriptDocument`. The delay usually occurs because the
  `Class.DataModel` is busy, and it's almost always extremely small, but it
  still exists.

  The existence of a `Class.ScriptDocument` indicates that a document is open in
  the Script Editor. All `Class.ScriptDocument` instances have
  `Class.ScriptEditorService` as its parent. Each instance adheres to the
  following encoding conventions:

  - All text in `Class.ScriptDocument` is UTF-8 encoded.
  - All line indices are 1-indexed.
  - All character indices are 1-indexed and count UTF-8 bytes, not graphemes, so
    the same warning from `Class.TextBox.CursorPosition` applies: many Unicode
    characters take more than one byte.
  - All ranges are inclusive of their start position and exclusive of their end
    position, so start == end implies an empty range.

  All APIs for `Class.ScriptDocument` are at **Plugin** level security.
code_samples: []
inherits:
  - Instance
descendants: []
tags:
  - NotCreatable
  - NotReplicated
deprecation_message: ''
properties: []
methods:
  - name: ScriptDocument:CloseAsync
    summary: |
      Requests that the editor associated with this document close. Yields the
      current thread until the editor responds to the request.
    description: |
      Requests that the editor associated with this document close. Yields the
      current thread until the editor responds to the request. If the function
      succeeds, it returns (true, nil). If the function fails, it returns
      (false, string) as a description of the problem.

      This function can't close the command bar.
    code_samples:
      - ScriptDocument-CloseAsync
    parameters: []
    returns:
      - type: Tuple
        summary: |
          A tuple where the first element is `true` if the editor closed
          successfully, or `false` followed by a string describing the failure
          reason.
    tags:
      - Yields
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:EditTextAsync
    summary: |
      Replaces the text in the specified range from (`startLine`, `startColumn`)
      to (`endLine`, `endColumn`) with newText.
    description: |
      Replaces the text in the specified range from (`startLine`, `startColumn`)
      to (`endLine`, `endColumn`) with `newText`. If the range is empty, then
      the function inserts the text at (`startLine`, `startColumn`). If the text
      cursor is within the specified range, the cursor moves to the end position
      of the edit. Otherwise, the text cursor doesn't move. This function yields
      the current thread until it receives a reply from the editor about the
      edit.

      If the function succeeds, it returns (`true`, `nil`).

      The function throws an error if:

      - The range is invalid.
      - The range would slice one Unicode character, for example replace only
        some of the bytes of the unicode character.
      - The `newText` itself contains invalid UTF-8.

      If the function fails, it returns (false, string). The string is a
      description of the problem. The most common failure type is a version
      mismatch. This occurs when you try to call `EditTextAsync` during the time
      when the `Class.ScriptDocument` is out of sync with the contents of the
      editor. If this happens, you can retry the edit.
    code_samples: []
    parameters:
      - name: newText
        type: string
        default:
        summary: |
          The replacement string to insert into the specified range.
      - name: startLine
        type: int
        default:
        summary: |
          The 1-indexed line number where the replacement range begins.
      - name: startCharacter
        type: int
        default:
        summary: |
          The 1-indexed UTF-8 byte offset within `startLine` where the
          replacement range begins.
      - name: endLine
        type: int
        default:
        summary: |
          The 1-indexed line number where the replacement range ends
          (exclusive).
      - name: endCharacter
        type: int
        default:
        summary: |
          The 1-indexed UTF-8 byte offset within `endLine` where the replacement
          range ends (exclusive).
    returns:
      - type: Tuple
        summary: |
          A tuple where the first element is `true` if the edit was applied
          successfully, or `false` followed by a string describing the failure
          reason.
    tags:
      - Yields
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:ForceSetSelectionAsync
    summary: |
      Asks the editor to set its cursor selection to the argument values.
    description: |
      Asks the editor to set its cursor selection to the argument values. Both
      anchor arguments must be passed, or neither. If neither is passed, then
      they each default to being the same as the corresponding cursor argument.
      The editor might decline to update its cursor if the text content of the
      document has changed. Unlike
      `Class.ScriptDocument:RequestSetSelectionAsync()`, the editor will not
      decline to move its cursor if the cursor has moved since the request was
      made. Returns (true, nil) if the cursor was updated, and (false, string)
      with an explanation string if it was not. Yields the current thread until
      the editor replies.
    code_samples:
      - ScriptDocument-ForceSetSelectionAsync
    parameters:
      - name: cursorLine
        type: int
        default:
        summary: |
          The 1-indexed line number for the cursor position.
      - name: cursorCharacter
        type: int
        default:
        summary: |
          The 1-indexed UTF-8 byte offset within the line for the cursor
          position.
      - name: anchorLine
        type: int?
        default: nil
        summary: |
          The 1-indexed line number for the anchor position. Defaults to
          `cursorLine` if not provided.
      - name: anchorCharacter
        type: int?
        default: nil
        summary: |
          The 1-indexed UTF-8 byte offset within the line for the anchor
          position. Defaults to `cursorCharacter` if not provided.
    returns:
      - type: Tuple
        summary: |
          A tuple where the first element is `true` if the cursor was updated,
          or `false` followed by a string explaining why the update was
          declined.
    tags:
      - Yields
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:GetLine
    summary: |
      Returns the text of the specified line. When no argument is provided,
      returns the line of the current cursor position.
    description: |
      Returns the text of the specified line. When no argument is provided,
      returns the line of the current cursor position.
    code_samples:
      - ScriptDocument-SelectionChanged-GetLine
    parameters:
      - name: lineIndex
        type: int?
        default: nil
        summary: |
          The 1-indexed line number to retrieve. Defaults to the current cursor
          line if not provided.
    returns:
      - type: string
        summary: |
          The text content of the specified line as a string.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:GetLineCount
    summary: |
      Returns the number of lines in the document.
    description: |
      Returns the number of lines in the active document.
    code_samples:
      - ScriptDocument-GetLineCount
    parameters: []
    returns:
      - type: int
        summary: |
          The total number of lines in the document.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:GetScript
    summary: |
      Returns the underlying `Class.LuaSourceContainer` instance, if one exists,
      otherwise `nil`.
    description: |
      Returns the underlying `Class.LuaSourceContainer` instance, if one exists,
      otherwise `nil`.
    code_samples:
      - ScriptDocument-GetScript
    parameters: []
    returns:
      - type: LuaSourceContainer
        summary: |
          The `Class.LuaSourceContainer` that the document is editing, or `nil`
          if the document does not represent a script instance in the place
          (e.g. the Command Bar is not a real script instance).
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:GetSelectedText
    summary: |
      Gets the text selected in the editor, or an empty string if there is no
      selection.
    description: |
      Gets the text selected in the editor, or an empty string if there is no
      selection.
    code_samples:
      - ScriptDocument-HasSelectedText-GetSelectedText
    parameters: []
    returns:
      - type: string
        summary: |
          The currently selected text as a string, or an empty string if nothing
          is selected.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:GetSelection
    summary: |
      Returns the last known selection of the Script Editor in the format:
      `CursorLine, CursorChar, AnchorLine, AnchorChar`. If the Script Editor has
      no selection, `CursorLine == AnchorLine` and `CursorChar == AnchorChar`.
    description: |
      Returns the last known selection of the Script Editor in the format:
      `CursorLine, CursorChar, AnchorLine, AnchorChar`. If the Script Editor has
      no selection, `CursorLine == AnchorLine` and `CursorChar == AnchorChar`.
    code_samples: []
    parameters: []
    returns:
      - type: Tuple
        summary: |
          CursorLine, CursorChar, AnchorLine, AnchorChar.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:GetSelectionEnd
    summary: |
      Gets the larger of the cursor position and anchor. If the editor has no
      selection, they are the same value.
    description: |
      Gets the larger of the cursor position and anchor. If the editor has no
      selection, they are the same value.
    code_samples:
      - ScriptDocument-GetSelectionStart-GetSelectionEnd
    parameters: []
    returns:
      - type: Tuple
        summary: |
          A tuple of (line, character) representing the larger of the cursor and
          anchor positions, both 1-indexed.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:GetSelectionStart
    summary: |
      Gets the smaller of the cursor position and anchor. If the editor has no
      selection, they are the same value.
    description: |
      Gets the smaller of the cursor position and anchor. If the editor has no
      selection, they are the same value.
    code_samples:
      - ScriptDocument-GetSelectionStart-GetSelectionEnd
    parameters: []
    returns:
      - type: Tuple
        summary: |
          A tuple of (line, character) representing the smaller of the cursor
          and anchor positions, both 1-indexed.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:GetText
    summary: |
      Returns text from the open editor.
    description: |
      Returns text from the open editor. Must be called with 0, 2 or 4
      arguments:

      - If called with 0 arguments, gets the entire contents of the open editor.
      - If called with 2 arguments, gets the text of the document starting at
        (`startLine`, `startColumn`).
      - If called with 4 arguments, gets the text of the document starting at
        (`startLine`, `startColumn`) and ending at (`endLine`, `endColumn`).
    code_samples:
      - ScriptDocument-GetText
    parameters:
      - name: startLine
        type: int?
        default: nil
        summary: |
          The 1-indexed line number where the text range begins. Optional; omit
          all arguments to get the entire document.
      - name: startCharacter
        type: int?
        default: nil
        summary: |
          The 1-indexed UTF-8 byte offset within `startLine` where the text
          range begins.
      - name: endLine
        type: int?
        default: nil
        summary: |
          The 1-indexed line number where the text range ends (exclusive).
          Optional; omit to read from `startLine`/`startCharacter` to the end of
          the document.
      - name: endCharacter
        type: int?
        default: nil
        summary: |
          The 1-indexed UTF-8 byte offset within `endLine` where the text range
          ends (exclusive).
    returns:
      - type: string
        summary: |
          The text content within the specified range as a string.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:GetViewport
    summary: |
      Returns the currently displayed line numbers in the editor change.
    description: |
      Returns the currently displayed line numbers in the editor change. The
      editor displays the lines between startLine and endLine, inclusive. The
      first and last line might only display partially. For example, only the
      topmost pixel of the last line might be on screen. Furthermore, code
      folding might hide lines between startLine and endLine.
    code_samples:
      - ScriptDocument-GetViewport
    parameters: []
    returns:
      - type: Tuple
        summary: |
          A tuple of (startLine, endLine) representing the 1-indexed range of
          lines currently visible in the editor.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:HasSelectedText
    summary: |
      Returns whether or not the editor has any text selected.
    description: |
      Returns whether or not the editor has any text selected.
    code_samples:
      - ScriptDocument-HasSelectedText-GetSelectedText
    parameters: []
    returns:
      - type: boolean
        summary: |
          `true` if the editor has a non-empty text selection, `false`
          otherwise.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:IsCommandBar
    summary: |
      Returns true if the `Class.ScriptDocument` represents the Command bar.
    description: |
      Returns true if the `Class.ScriptDocument` represents the Command bar. The
      command bar has special rules and limitations in this API:

      - Studio creates the Command bar before running plugins, so it doesn't
        always fire the opened event, although it does close and reopen as
        Studio transitions between DataModels.
      - You can't edit the Command bar with `EditTextAsync` for security
        reasons.
    code_samples:
      - ScriptDocument-IsCommandBar
    parameters: []
    returns:
      - type: boolean
        summary: |
          `true` if this document represents the Command bar, `false` otherwise.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:MultiEditTextAsync
    summary: |
      Applies a batch of text edits to the document as a single atomic
      operation. Yields the current thread until the editor responds.
    description: |
      Applies a batch of text edits to the document as a single atomic
      operation, then yields the current thread until it receives a reply from
      the editor. Each entry in the `edits` array is a table describing one
      replacement, with the same meaning as the arguments to
      `Class.ScriptDocument:EditTextAsync()`, and the following fields:

      - `NewText`: The replacement text.
      - `StartLine`: The line of the start of the range to replace.
      - `StartCharacter`: The character of the start of the range to replace.
      - `EndLine`: The line of the end of the range to replace.
      - `EndCharacter`: The character of the end of the range to replace.

      The edits are applied together, so either all of them succeed or none of
      them are applied. Before applying any edit, the function validates every
      edit and throws an error if:

      - Any range is invalid.
      - Any range would slice one Unicode character, for example replace only
        some of the bytes of the unicode character.
      - Any `NewText` itself contains invalid UTF-8.
      - The ranges of two edits overlap.

      If the function succeeds, it returns (`true`, `nil`). If the function
      fails, it returns (`false`, `string`), where the string describes the
      problem. As with `Class.ScriptDocument:EditTextAsync()`, the most common
      failure is a version mismatch, which occurs when the
      `Class.ScriptDocument` is momentarily out of sync with the contents of the
      editor; if this happens, you can retry the edits.
    code_samples: []
    parameters:
      - name: edits
        type: Array
        default:
        summary: |
          An array of tables, each describing a text replacement with fields
          `NewText`, `StartLine`, `StartCharacter`, `EndLine`, and
          `EndCharacter`.
    returns:
      - type: Tuple
        summary: |
          A tuple where the first element is `true` if all edits were applied
          successfully, or `false` followed by a string describing the failure
          reason.
    tags:
      - Yields
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:RequestSetSelectionAsync
    summary: |
      Asks the editor to set its cursor selection to the argument values.
    description: |
      Asks the editor to set its cursor selection to the argument values. Both
      anchor arguments must be passed, or neither. If neither is passed, then
      they each default to being the same as the corresponding cursor argument.
      The editor might decline to update its cursor if the text content of the
      document has changed, or the cursor has moved since the request was made.
      Returns (true, nil) if the cursor was updated, and (false, string) with an
      explanation string if it was not. Yields the current thread until the
      editor replies.
    code_samples:
      - ScriptDocument-RequestSetSelectionAsync
    parameters:
      - name: cursorLine
        type: int
        default:
        summary: |
          The 1-indexed line number for the cursor position.
      - name: cursorCharacter
        type: int
        default:
        summary: |
          The 1-indexed UTF-8 byte offset within the line for the cursor
          position.
      - name: anchorLine
        type: int?
        default: nil
        summary: |
          The 1-indexed line number for the anchor position. Defaults to
          `cursorLine` if not provided.
      - name: anchorCharacter
        type: int?
        default: nil
        summary: |
          The 1-indexed UTF-8 byte offset within the line for the anchor
          position. Defaults to `cursorCharacter` if not provided.
    returns:
      - type: Tuple
        summary: |
          A tuple where the first element is `true` if the cursor was updated,
          or `false` followed by a string explaining why the update was
          declined.
    tags:
      - Yields
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
  - name: ScriptDocument:ReviewableTextEditsAsync
    summary: |
      Applies a batch of line-based text edits that appear in the editor as
      reviewable inline diffs, allowing users to accept or reject each change.
    description: |
      Applies a batch of line-based text edits to the document that are
      presented to the user as reviewable inline diffs in the Script Editor
      gutter. Each entry in the `changes` array is a table with the following
      fields:

      - `Text`: The replacement text to insert. Must be a non-empty string
        unless `EndLine` is provided.
      - `StartLine`: The 1-indexed line number where the edit begins. If
        `EndLine` is omitted, the text is inserted before this line. If
        `EndLine` is provided, lines from `StartLine` through `EndLine` are
        replaced.
      - `EndLine`: Optional. The 1-indexed last line of the range to replace
        (inclusive). Must be greater than or equal to `StartLine`.

      The edits must not overlap or border each other; there must be at least
      one unchanged line between any two edits. Edits are applied atomically.
      The function throws an error if any entry has invalid fields. If the
      function fails due to a version mismatch or other editor-side issue, it
      returns (`false`, `string`).

      This method cannot be used on the Command bar.
    code_samples: []
    parameters:
      - name: changes
        type: Array
        default:
        summary: |
          An array of tables, each with a `Text` field (the replacement string),
          a `StartLine` field (1-indexed line to insert or begin replacing at),
          and an optional `EndLine` field (1-indexed last line to replace,
          inclusive).
    returns:
      - type: Tuple
        summary: |
          A tuple where the first element is `true` if the edits were applied
          successfully, or `false` followed by a string describing the failure
          reason.
    tags:
      - Yields
    deprecation_message: ''
    security: PluginSecurity
    thread_safety: Unsafe
    capabilities: []
events:
  - name: ScriptDocument.SelectionChanged
    summary: |
      Fires when the ScriptDocument changes, including immediately after a text
      change.
    description: |
      Fires when the ScriptDocument changes, including immediately after a text
      change.
    code_samples:
      - ScriptDocument-SelectionChanged-GetLine
    parameters:
      - name: positionLine
        type: int64
        default:
        summary: |
          The 1-indexed line number of the cursor position after the change.
      - name: positionCharacter
        type: int64
        default:
        summary: |
          The 1-indexed UTF-8 byte offset of the cursor position after the
          change.
      - name: anchorLine
        type: int64
        default:
        summary: |
          The 1-indexed line number of the selection anchor after the change.
      - name: anchorCharacter
        type: int64
        default:
        summary: |
          The 1-indexed UTF-8 byte offset of the selection anchor after the
          change.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    capabilities: []
  - name: ScriptDocument.ViewportChanged
    summary: |
      Fires when the displayed line numbers in the editor change.
    description: |
      Fires when the displayed line numbers in the editor change. See
      `Class.ScriptDocument.GetViewport` for details.
    code_samples:
      - ScriptDocument-ViewportChanged
    parameters:
      - name: startLine
        type: int64
        default:
        summary: |
          The 1-indexed first line currently visible in the editor viewport.
      - name: endLine
        type: int64
        default:
        summary: |
          The 1-indexed last line currently visible in the editor viewport.
    tags: []
    deprecation_message: ''
    security: PluginSecurity
    capabilities: []
callbacks: []
