name: UIPadding
type: class
category: GUI
memory_category: Instances
summary: |
  Applies padding to the borders of the parent `Class.GuiObject`.
description: |
  Applies padding to the borders of the parent `Class.GuiObject`, either as a
  pixel amount (`Datatype.UDim.Offset|Offset`) or percentage of the parent's
  size (`Datatype.UDim.Scale|Scale`).
code_samples:
inherits:
  - UIComponent
tags: []
deprecation_message: ''
properties:
  - name: UIPadding.PaddingBottom
    summary: |
      Padding to apply on the bottom side, relative to the parent's normal size.
    description: |
      Padding to apply on the bottom side, relative to the parent's normal size.
    code_samples:
    type: UDim
    tags: []
    deprecation_message: ''
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Behavior
    serialization:
      can_load: true
      can_save: true
  - name: UIPadding.PaddingLeft
    summary: |
      Padding to apply on the left side, relative to the parent's normal size.
    description: |
      Padding to apply on the left side, relative to the parent's normal size.
    code_samples:
    type: UDim
    tags: []
    deprecation_message: ''
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Behavior
    serialization:
      can_load: true
      can_save: true
  - name: UIPadding.PaddingRight
    summary: |
      Padding to apply on the right side, relative to the parent's normal size.
    description: |
      Padding to apply on the right side, relative to the parent's normal size.
    code_samples:
    type: UDim
    tags: []
    deprecation_message: ''
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Behavior
    serialization:
      can_load: true
      can_save: true
  - name: UIPadding.PaddingTop
    summary: |
      Padding to apply on the top side, relative to the parent's normal size.
    description: |
      Padding to apply on the top side, relative to the parent's normal size.
    code_samples:
    type: UDim
    tags: []
    deprecation_message: ''
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Behavior
    serialization:
      can_load: true
      can_save: true
methods: []
events: []
callbacks: []
