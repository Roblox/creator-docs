name: Message
type: class
category:
memory_category: Instances
summary: |
  Fills the entire screen with a semi-transparent grey background, with centered
  text in the middle of the screen. With the introduction of Roblox's GUI
  features it has been deprecated and `Class.TextLabel|TextLabels` should be
  used instead for new work.
description: |
  Fills the entire screen with a semi-transparent grey background, with centered
  text in the middle of the screen. With the introduction of Roblox's GUI
  features it has been deprecated and `Class.TextLabel|TextLabels` should be
  used instead for new work.

  Messages will not display if the `Class.Message.Text` field is empty.
code_samples:
inherits:
  - Instance
tags:
  - Deprecated
deprecation_message: |
  With the introduction of Roblox's GUI features hints have been deprecated and
  `Class.TextLabel|TextLabels` should be used instead for new work. The
  `Class.TextLabel` object offers a wide range of features for displaying and
  customizing text that messages do not.
properties:
  - name: Message.Text
    summary: |
      Sets the text of a `Class.Message` or `Class.Hint`.
    description: |
      Sets the text of a `Class.Message` or `Class.Hint`.

      A `Class.Message` will be invisible if its `Class.Message.Text` property
      is blank, however the black bar of a `Class.Hint` will remain visible.
    code_samples:
    type: string
    tags: []
    deprecation_message: |
      `Class.Message|Messages` and `Class.Hint|Hints` have been deprecated;
      `Class.TextLabel|TextLabels` should be used instead in new work.
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Appearance
    serialization:
      can_load: true
      can_save: true
methods: []
events: []
callbacks: []
