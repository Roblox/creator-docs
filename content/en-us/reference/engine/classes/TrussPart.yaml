name: TrussPart
type: class
category: Building
memory_category: Instances
summary: |
  Similar to a `Class.Part` but with a different visual
  `Class.TrussPart.Style|Style` and the important distinction that default
  characters are able to climb it.
description: |
  A **TrussPart** is similar to a `Class.Part` but with a different visual
  `Class.TrussPart.Style|Style` and the important distinction that default
  characters are able to climb it.

  This part type has significantly more restrictive size limitations than other
  parts; it must be `2 * 2 * n` studs where `n` is a multiple of 2 with a
  maximum of 512.
code_samples:
inherits:
  - BasePart
tags: []
deprecation_message: ''
properties:
  - name: TrussPart.Style
    summary: |
      Sets what the truss looks like.
    description: |
      Sets what the truss looks like, from the following options:

      - `Enum.Style.AlternatingSupports`
      - `Enum.Style.BridgeStyleSupports`
      - `Enum.Style.NoSupports`
    code_samples:
    type: Style
    tags:
      - NotReplicated
    deprecation_message: ''
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Part
    serialization:
      can_load: true
      can_save: false
methods: []
events: []
callbacks: []
