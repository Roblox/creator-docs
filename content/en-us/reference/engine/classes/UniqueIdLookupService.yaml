# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: UniqueIdLookupService
type: class
memory_category: Instances
summary: |
  Team Create service that maps `Class.Instance.UniqueId|UniqueId` to and from
  RFC-4122 strings; not for experience scripts.
description: |
  Not intended for experience scripts. Used by Team Create and Studio
  collaboration to map between an `Class.Instance`'s
  `Class.Instance.UniqueId|UniqueId` (a 128-bit identifier) and its canonical
  RFC-4122 string form, and to resolve the live instance from that string.
code_samples: []
inherits:
  - Instance
descendants: []
tags:
  - NotCreatable
  - Service
deprecation_message: ''
properties: []
methods:
  - name: UniqueIdLookupService:GetOrCreateUniqueIdRemoteCommand
    summary: |
      Returns the RFC-4122 string form of the instance's
      `Class.Instance.UniqueId|UniqueId`, creating one if the instance does not
      already have it.
    description: |
      Returns the RFC-4122 string representation of the given `Class.Instance`'s
      `Class.Instance.UniqueId|UniqueId`. If the instance does not yet have one,
      a `Class.Instance.UniqueId|UniqueId` is generated for it (and for any
      descendants that lack one) before the string is returned.
    code_samples: []
    parameters:
      - name: instance
        type: Instance
        default:
        summary: |
          The instance whose `Class.Instance.UniqueId|UniqueId` to read or
          create.
    returns:
      - type: string
        summary: |
          The instance's `Class.Instance.UniqueId|UniqueId` as an RFC-4122
          string.
    tags: []
    deprecation_message: ''
    security: None
    thread_safety: Unsafe
    capabilities:
      - Unknown
events: []
callbacks: []
