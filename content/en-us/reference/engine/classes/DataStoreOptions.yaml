# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: DataStoreOptions
type: class
memory_category: Instances
summary: |
  Object used to provide additional parameters to
  `Class.DataStoreService:GetDataStore()`.
description: |
  A configuration object passed as the third argument to
  `Class.DataStoreService:GetDataStore()` to customize how the returned
  `Class.DataStore` behaves. Create one with `Instance.new("DataStoreOptions")`,
  set its properties, then pass it to `GetDataStore`. Currently the only
  behavioral property is `Class.DataStoreOptions.AllScopes`.
code_samples: []
inherits:
  - Instance
descendants: []
tags:
  - NotReplicated
deprecation_message: ''
properties:
  - name: DataStoreOptions.AllScopes
    summary: |
      Whether the `Class.GlobalDataStore` should work with all scopes.
    description: |
      When set to `true`, the `Class.DataStore` returned by
      `Class.DataStoreService:GetDataStore()` operates across all scopes instead
      of a single scope. The `scope` parameter (second argument) to
      `GetDataStore` must be an empty string (`""`) when this property is
      enabled; passing a non-empty scope throws an error.

      In all-scopes mode, every key is prefixed with its scope and a slash
      separator. For example, `Class.DataStore:ListKeysAsync()` returns keys
      such as `global/player_data_1234` or `houses/house3`. When you create or
      access a key, you must include the scope prefix in the format
      `scope/keyname`; omitting the slash throws an error.

      Default value is `false`.
    code_samples: []
    type: boolean
    tags: []
    deprecation_message: ''
    security:
      read: None
      write: None
    thread_safety: ReadSafe
    category: Data
    serialization:
      can_load: true
      can_save: true
    capabilities:
      - DataStore
methods:
  - name: DataStoreOptions:SetExperimentalFeatures
    summary: |
      Currently has no observable effect on data store behavior.
    description: |
      Accepts a dictionary of feature-name keys mapped to boolean values and
      stores them on this `Class.DataStoreOptions` instance. Calling this method
      has no observable effect on runtime behavior.
    code_samples: []
    parameters:
      - name: experimentalFeatures
        type: Dictionary
        default:
        summary: |
          Luau table in `key = value` format where the key is the experimental
          feature name and the value is a boolean which specifies whether to
          enable.
    returns:
      - type: ()
        summary: ''
    tags: []
    deprecation_message: ''
    security: None
    thread_safety: Unsafe
    capabilities:
      - DataStore
events: []
callbacks: []
