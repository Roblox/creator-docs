# This file is automatically generated. Please don't edit it manually.
# To submit a bug report on the content, see
# https://devforum.roblox.com/c/bug-reports/documentation-issues/72

name: Actor
type: class
memory_category: BaseParts
summary: |
  An `Class.Actor` is a container for code that can be safely split into its own
  thread.
description: |
  An `Class.Actor` is a container for code that can be safely split into its own
  thread using `Library.task.desynchronize()`. It should also contain the
  instances used by its scripts.

  To learn more about using multiple Actors to optimize script performance, see
  [Parallel Luau](../../../scripting/multithreading.md).

  #### Notes

  - Each `Actor` runs in its own Luau VM. `Class.ModuleScript|ModuleScripts`
    required by an `Actor` are **not** shared or cached across `Actors` or with
    the main thread. Each VM executes its own copy of the module, so
    module-level state is isolated per `Actor`. Design accordingly if your
    modules hold state you intend to share.
  - `script:GetActor()` returns `nil` when called from a `Class.ModuleScript`
    unless the `ModuleScript` is a descendant of the `Actor`. `ModuleScripts`
    stored elsewhere (for example, `Class.ReplicatedStorage`) and required by an
    `Actor` are not considered descendants of the `Actor`.
code_samples: []
inherits:
  - Model
descendants: []
tags: []
deprecation_message: ''
properties: []
methods:
  - name: Actor:BindToMessage
    summary: |
      Binds a Luau callback to a message with the specified topic.
    description: |
      This method is used to bind a Luau callback to a message with the
      specified topic. When a message is sent (using
      `Class.Actor:SendMessage()|SendMessage()`) to the topic specified the
      provided callback will be called in a _serial_ execution context.

      Multiple Luau callbacks may be bound to a single actor and even to a
      single message topic.

      Note: Only the scripts which are descendants of an Actor may bind to its
      messages.

      ```lua
      local actor = script:GetActor()

      -- Print out a message when a greeting message is sent to the Actor
      -- this script is a descendant of.
      local connection = actor:BindToMessage("Greeting", function(message)
        print("Received Greeting Message:", message)
      end)
      ```
    code_samples: []
    parameters:
      - name: topic
        type: string
        default:
        summary: |
          The topic used to identify the type of message.
      - name: function
        type: Function
        default:
        summary: ''
    returns:
      - type: RBXScriptConnection
        summary: |
          This connection object may be used to disconnect the Luau callback
          from receiving messages.
    tags: []
    deprecation_message: ''
    security: None
    thread_safety: Safe
    capabilities:
      - Basic
  - name: Actor:BindToMessageParallel
    summary: |
      Binds a Luau callback to a message with the specified topic.
    description: |
      This method is used to bind a Luau callback to a message with the
      specified topic. When a message is sent (using
      `Class.Actor:SendMessage()|SendMessage()`) to the topic specified the
      provided callback will be called in a _parallel_ execution context.

      Multiple Luau callbacks may be bound to a single actor and even to a
      single message topic.

      Note: Only the scripts which are descendants of an Actor may bind to its
      messages.

      ```lua
      local actor = script:GetActor()

      -- Print out a message when a greeting message is sent to the Actor
      -- this script is a descendant of.
      local connection = actor:BindToMessageParallel("Greeting", function(message)
        print("Received Greeting Message:", message)
      end)
      ```
    code_samples: []
    parameters:
      - name: topic
        type: string
        default:
        summary: |
          The topic used to identify the type of message.
      - name: function
        type: Function
        default:
        summary: ''
    returns:
      - type: RBXScriptConnection
        summary: |
          This connection object may be used to disconnect the Luau callback
          from receiving messages.
    tags: []
    deprecation_message: ''
    security: None
    thread_safety: Safe
    capabilities:
      - Basic
  - name: Actor:SendMessage
    summary: |
      Sends a message to an Actor.
    description: |
      Sends a message to an Actor. Messages are sent asynchronously, so the
      sender will not block or yield when calling the
      `Class.Actor:SendMessage()|SendMessage()` method.

      Since a single Actor may receive different kinds of messages, a `topic`
      parameter is used to distinguish between various kinds of messages.

      See `Class.Actor:BindToMessage()|BindToMessage()` for details on receiving
      a message sent using `Class.Actor:SendMessage()|SendMessage()`.

      Note that message arguments are **passed by copy** across Luau VM
      boundaries, not by reference. Sending large tables incurs a full copy on
      each call. Functions are bound to a specific VM and cannot be passed in
      messages.

      ```lua
      -- Assume `actor` is a local variable referring to an Actor instance
      actor:SendMessage("Greeting", "Hello World")
      ```
    code_samples: []
    parameters:
      - name: topic
        type: string
        default:
        summary: |
          The topic used to identify the message being sent.
      - name: message
        type: Tuple
        default:
        summary: |
          The contents of the message to send to the Actor.
    returns:
      - type: ()
        summary: ''
    tags: []
    deprecation_message: ''
    security: None
    thread_safety: Safe
    capabilities:
      - Basic
events: []
callbacks: []
